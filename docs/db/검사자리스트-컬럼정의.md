# 검사자 리스트 컬럼 정의서

> SoftCRM **검사자 리스트** 계열 엑셀 출력(75/78컬럼) ↔ java_dashboard API 매핑 문서  
> DB 기준: MSSQL(SOFTCRM), READ-ONLY, 대용량 테이블 `WITH(NOLOCK)` + 날짜 범위 필수  
> 최종 갱신: 2026-06-16

> **개발자용 요약**: 화면에 실제로 뜨는 컬럼은 [화면 표시 컬럼](#화면-표시-컬럼-실제-화면-기준)(51열), 실제 실행 SQL 전문은 [호출 쿼리문](#호출-쿼리문-실제-실행-sql) 참조. 아래 [컬럼 매핑](#컬럼-매핑)은 원본 엑셀 78열 기준 추적표.

## 요약

| 항목 | 내용 |
|---|---|
| 화면명 | 검사자 리스트 (상담사별) |
| 원본 계열 | `FrmCounselExamList` 구 25컬럼 + 확장 75/78컬럼 SQL |
| 주 데이터 | `EXAM` + `CUSTOM` |
| 기간 기준 | `EXAM.EXAM_DATE` |
| 현재 API | `GET /api/exam-list?from=YYYY-MM-DD&to=YYYY-MM-DD` |
| 현재 구현 | 시력교정 `EXAM` 분기 중심. 백내장 본분기 `UNION` 미구현 |
| 프론트 표시 | 검사정보 측정값 제외, 비측정 47필드 표시 |

> **주의:** 이 화면의 “검사자”는 사람 배정 현황이 아님.  
> 고객별 검사자료 + 예약 + 상담 + 수술 관련 마스터 리스트에 가까움. `uASSIGN1` 검사배정현황과 혼동 금지.

## 출력 기준

java_dashboard 검사자 리스트 API 기준:

- 기간 내 `EXAM.EXAM_DATE` 행 조회
- 상담사/상담의 제한 없이 전체 조회
- 진료구분/예약 부가정보는 검사일과 같은 날의 검사예약 기준
  - `RESERVE_FLAG='M'`
  - `RESERVE_DATE = EXAM.EXAM_DATE`
  - `RESERVE_STATE IN ('I','H')`
- 검사분류는 별도 파생값
  - 같은 검사일에 렌즈센터 예약 `D`만 있고 검사예약 `M`이 없으면 `드림렌즈`
  - 그 외 `시력교정`
- 테스트 데이터 제외
  - 고객명에 `테스트`, `test`, `TEST` 포함 시 제외
  - 검사메모/예약번호의 `test` 문구는 제외 기준으로 사용하지 않음
  - `장용테스트`, `모노비전TEST` 같은 정상 임상 메모 오탐 방지 목적
- 백내장 검사 그림자 행 제외
  - 같은 고객/검사일에 `Cataract_Exam` 본행 존재
  - 일반 `EXAM.RIGHT01~RIGHT30/LEFT01~LEFT30` 측정값 전부 공백
  - 위 조건이면 시력교정 검사자 리스트에서 제외
- 모든 응답 필드는 문자열 보장
  - 프론트 Zod 스키마가 `z.string()` 기준이라 `null` 금지

## 화면 표시 컬럼 (실제 화면 기준)

> 출처: [ExamListPage.tsx](../../frontend/src/pages/ExamListPage.tsx) `COLUMNS` 정의.  
> 화면 표시 **51열** = API 응답 **49필드** + `No`(행번호)·`만나이`(생년월일 파생) 2열.  
> 표시 순서는 아래 표 순서와 동일. `필드`는 API JSON 키(= SQL alias).

| # | 화면 헤더 | API 필드 | 원본 컬럼(#) | 표시 처리 |
|---:|---|---|---|---|
| 1 | No | — | — | 행번호(조회순). API 무관 |
| 2 | 검사일 | `examDate` | 검사일(4) | |
| 3 | 검사분류 | `examCategory` | 파생 | 시력교정/드림렌즈 뱃지 |
| 4 | 신/구환 | `patientType` | 파생 | `FIRST_DAY`가 조회기간 내면 신환 |
| 5 | 진료구분 | `examType` | 검사예약진료구분(5) | 코드→라벨 뱃지 |
| 6 | 검사시간 | `examTime` | 검사예약시간(40) | |
| 7 | 차트번호 | `chartNo` | 고객번호(1) | |
| 8 | 고객명 | `name` | 고객명(2) | |
| 9 | 고객명(영) | `nameEng` | 고객명(영)(3) | |
| 10 | 등급 | `grade` | 등급(73) | 뱃지 |
| 11 | 생년월일 | `birth` | 생일(53) | |
| 12 | 만나이 | — | 만나이(75) | `birth`에서 프론트 계산 |
| 13 | 양/음 | `lunar` | 양/음(54) | `LUNAR_YN='Y'`→음 |
| 14 | 휴대전화 | `phone2` | 휴대전화(56) | |
| 15 | 집전화 | `phone1` | 집전화(55) | |
| 16 | 이메일 | `email` | 이메일(57) | |
| 17 | 상담사 | `counselor` | 상담사(50) | |
| 18 | 상담의 | `doctor` | 상담의(51) | |
| 19 | 검안사 | `optometrist` | 검안사(70) | |
| 20 | 적절수술R | `recommendedR` | 적절한수술방법R(41) | |
| 21 | 적절수술L | `recommendedL` | 적절한수술방법L(42) | |
| 22 | 수술예약일 | `surgeryReserveDate` | 수술예약일(44) | |
| 23 | 수술일 | `surgeryDate` | 수술일(45) | |
| 24 | 수술방법R | `surgeryR` | 수술방법R(46) | |
| 25 | 수술방법L | `surgeryL` | 수술방법L(47) | |
| 26 | 집도의 | `surgeon` | 집도의(48) | |
| 27 | 견적가 | `estimate` | 견적가(38) | |
| 28 | 수술가율 | `surgeryRate` | 수술가율(39) | |
| 29 | 수납금액 | `payment` | 수납금액(49) | 결제 집계 `RECEIPT` |
| 30 | 수술불가 | `opImpossible` | 수술불가구분(63) | Y/— |
| 31 | 검사중단 | `examStop` | 검사중단구분(62) | Y/— |
| 32 | 취소사유 | `cancelCode` | 취소사유(71) | |
| 33 | 취소메모 | `cancelMemo` | 취소사유메모(72) | |
| 34 | 예약경로 | `route` | 예약경로(64) | |
| 35 | 섹션 | `section` | 섹션(65) | |
| 36 | 내원동기(대) | `motiveL` | 내원동기(대)(66) | |
| 37 | 내원동기(중) | `motiveM` | 내원동기(중)(67) | |
| 38 | 내원동기(세) | `motiveS` | 내원동기(세)(68) | |
| 39 | 동기메모 | `motiveMemo` | 내원동기메모(69) | |
| 40 | 직업 | `job` | 직업(74) | |
| 41 | 국적 | `nationality` | 국적(78) | |
| 42 | 보험사 | `insurance` | 보험사(77) | |
| 43 | 주민번호 | `jumin` | 주민번호(52) | PII → 항상 빈 값 |
| 44 | 우편번호 | `zip` | 우편번호(58) | |
| 45 | 주소1 | `addr1` | 주소1(59) | |
| 46 | 주소2 | `addr2` | 주소2(60) | |
| 47 | 검사예약등록일 | `examRegDate` | 검사예약등록일(6) | |
| 48 | 수술예약등록일 | `surgeryRegDate` | 수술예약등록일(43) | |
| 49 | 최근내원일 | `lastVisit` | 최근내원일(76) | |
| 50 | 검사특이사항 | `examMemo` | 검사특이사항(37) | |
| 51 | 고객메모 | `memo` | 고객메모(61) | |

> 화면 미표시(API 미반환): 검사정보1~30(측정값, 원본 7~36). 화면은 비측정 컬럼만 보여줌.

## 호출 쿼리문 (실제 실행 SQL)

> 출처: [ExamListRepository.java](../../backend/src/main/java/com/bviit/analytics/repository/exam/ExamListRepository.java) `findExamList(from, to)`.  
> 엔드포인트: `GET /api/exam-list?from=YYYY-MM-DD&to=YYYY-MM-DD` (Profile `mssql`).  
> 파라미터: `:from`, `:to` = 검사일 범위(`EXAM.EXAM_DATE`, char(10)).  
> `NamedParameterJdbcTemplate.queryForList` → `List<Map<String,Object>>` 그대로 응답(DTO 없음, alias=JSON 키).

```sql
SELECT
  ISNULL(LTRIM(RTRIM(cu.CUST_NUM)), '')        AS chartNo,
  ISNULL(cu.CUST_NAME, '')                     AS name,
  ISNULL(cu.CUST_ENAME, '')                    AS nameEng,
  ISNULL(e.EXAM_DATE, '')                      AS examDate,
  ISNULL(
    NULLIF(LTRIM(RTRIM(ms.SUB_NAME)), ''),
    CASE
      WHEN ISNULL(rsv.RESERVE_JINRYO,'')='1' THEN '검사'
      WHEN ISNULL(rsv.RESERVE_JINRYO,'')='5' THEN '검사OP'
      WHEN ISNULL(rsv.RESERVE_JINRYO,'')='4' THEN 'DNA검사'
      WHEN ISNULL(rsv.RESERVE_JINRYO,'')='2' THEN '재검사'
      WHEN ISNULL(rsv.RESERVE_JINRYO,'')='6' THEN '당일재검사'
      WHEN ISNULL(rsv.RESERVE_JINRYO,'')='7' THEN '검사OP(Re)'
      ELSE ISNULL(rsv.RESERVE_JINRYO, '')
    END
  )                                             AS examType,
  CASE WHEN visit.hasDreamlens = 1 AND visit.hasMedical = 0 THEN '드림렌즈' ELSE '시력교정' END AS examCategory,
  CASE WHEN ISNULL(cu.FIRST_DAY, '') >= :from AND ISNULL(cu.FIRST_DAY, '') <= :to
       THEN '신환' ELSE '구환' END             AS patientType,
  ISNULL(rsvReg.regDate, '')                   AS examRegDate,
  ISNULL(e.EXAM_MEMO, '')                      AS examMemo,
  ISNULL(e.OP_ESTIMATE, '')                    AS estimate,
  ISNULL(e.ExamPercent, '')                    AS surgeryRate,
  ISNULL(rsv.START_TIME, '')                   AS examTime,
  ISNULL(e.OPERATIONR, '')                     AS recommendedR,
  ISNULL(e.OPERATIONL, '')                     AS recommendedL,
  ISNULL(opReg.regDate, '')                    AS surgeryRegDate,
  ISNULL(opRsv.RESERVE_DATE, '')               AS surgeryReserveDate,
  ISNULL(op.OPERATION_DATE, '')                AS surgeryDate,
  ISNULL(op.OPERATIONR, '')                    AS surgeryR,
  ISNULL(op.OPERATIONL, '')                    AS surgeryL,
  ISNULL(os.EMP_NAME, '')                      AS surgeon,
  ISNULL(REPLACE(CONVERT(varchar(30), CONVERT(money, NULLIF(pay.RECEIPT, 0)), 1), '.00', ''), '') AS payment,
  ISNULL(ec.EMP_NAME, '')                      AS counselor,
  ISNULL(ed.EMP_NAME, '')                      AS doctor,
  ''                                           AS jumin,              -- PII: 복호화/마스킹 필요
  ISNULL(cu.BIRTH_DAY, '')                     AS birth,
  CASE WHEN cu.LUNAR_YN = 'Y' THEN '음' ELSE '양' END AS lunar,
  ISNULL(cu.CALL_NUM1, '')                     AS phone1,
  ISNULL(cu.CALL_NUM2, '')                     AS phone2,
  ISNULL(cu.EMAIL, '')                         AS email,
  ISNULL(LTRIM(RTRIM(cu.ZIP_CODE)), '')        AS zip,
  ISNULL(cu.ADDR1, '')                         AS addr1,
  ISNULL(cu.ADDR2, '')                         AS addr2,
  ISNULL(cu.ETC, '')                           AS memo,
  ISNULL(e.STOP_YN, '')                        AS examStop,
  ISNULL(e.OP_POSSIBLE_YN, '')                 AS opImpossible,
  ISNULL(rsv.RESERVE_PATH, '')                 AS route,
  ISNULL(motive.section, '')                   AS section,
  ISNULL(motive.motiveL, '')                   AS motiveL,
  ISNULL(motive.motiveM, '')                   AS motiveM,
  ISNULL(motive.motiveS, '')                   AS motiveS,
  ISNULL(motive.motiveMemo, '')                AS motiveMemo,
  ISNULL(eo.EMP_NAME, '')                      AS optometrist,
  ISNULL(LTRIM(RTRIM(e.CANCEL_CD)), '')        AS cancelCode,
  ISNULL(NULLIF(e.CANCEL_REASON, ''), ISNULL(cc.CANCEL_REASON, '')) AS cancelMemo,
  ISNULL(cu.LEVEL, '')                         AS grade,
  ISNULL(cu.JOB, '')                           AS job,
  ISNULL(cu.LAST_DAY, '')                      AS lastVisit,
  ISNULL(ins.EtcCodNam, '')                    AS insurance,
  ISNULL(nation.Nation_CodeName, '')           AS nationality
FROM EXAM e WITH(NOLOCK)
JOIN CUSTOM cu WITH(NOLOCK)              ON e.CUST_NUM = cu.CUST_NUM
LEFT JOIN OPERATIONDATA op WITH(NOLOCK)  ON e.CUST_NUM = op.CUST_NUM
LEFT JOIN EMPLOYEE ec WITH(NOLOCK)       ON cu.MY_COUNSELOR   = ec.EMP_NUM
LEFT JOIN EMPLOYEE ed WITH(NOLOCK)       ON cu.MY_DOCTOR      = ed.EMP_NUM
LEFT JOIN EMPLOYEE eo WITH(NOLOCK)       ON cu.MY_OPTOMETRIST = eo.EMP_NUM
LEFT JOIN EMPLOYEE os WITH(NOLOCK)       ON op.OPERATION_DOC  = os.EMP_NUM
LEFT JOIN CANCEL_CFG cc WITH(NOLOCK)     ON e.CANCEL_CD       = cc.CANCEL_CD
LEFT JOIN NationCustom_CFG nation WITH(NOLOCK) ON cu.Nation_code = nation.Code
LEFT JOIN EtcCfg ins WITH(NOLOCK)
  ON ins.EtcBseCod = 'InsuranceCfg'
 AND ins.EtcCod = cu.InsuranceCode
OUTER APPLY (
  SELECT
    CASE WHEN EXISTS (
      SELECT 1 FROM RESERVATION rd WITH(NOLOCK)
      WHERE rd.CUST_NUM = e.CUST_NUM
        AND rd.RESERVE_DATE = e.EXAM_DATE
        AND rd.RESERVE_STATE IN ('I','H')
        AND rd.RESERVE_FLAG = 'D'
    ) THEN 1 ELSE 0 END AS hasDreamlens,
    CASE WHEN EXISTS (
      SELECT 1 FROM RESERVATION rm WITH(NOLOCK)
      WHERE rm.CUST_NUM = e.CUST_NUM
        AND rm.RESERVE_DATE = e.EXAM_DATE
        AND rm.RESERVE_STATE IN ('I','H')
        AND rm.RESERVE_FLAG = 'M'
    ) THEN 1 ELSE 0 END AS hasMedical
) visit
OUTER APPLY (
  SELECT TOP 1 r.*
  FROM RESERVATION r WITH(NOLOCK)
  WHERE r.CUST_NUM = e.CUST_NUM
    AND r.RESERVE_DATE = e.EXAM_DATE
    AND r.RESERVE_STATE IN ('I','H')
    AND r.RESERVE_FLAG = 'M'
  ORDER BY r.RESERVE_NUM DESC
) rsv
LEFT JOIN MEDICAL_SUB_CFG ms WITH(NOLOCK)
  ON ms.RESERVE_FLAG = rsv.RESERVE_FLAG
 AND ms.SUB_FLAG = ISNULL(rsv.RESERVE_JINRYO, '')
OUTER APPLY (
  SELECT CASE WHEN LEN(LTRIM(RTRIM(ISNULL(rsv.RESERVE_NUM, '')))) >= 6
              THEN '20' + SUBSTRING(LTRIM(RTRIM(rsv.RESERVE_NUM)), 1, 2)
                 + '-' + SUBSTRING(LTRIM(RTRIM(rsv.RESERVE_NUM)), 3, 2)
                 + '-' + SUBSTRING(LTRIM(RTRIM(rsv.RESERVE_NUM)), 5, 2)
              ELSE '' END AS regDate
) rsvReg
OUTER APPLY (
  SELECT TOP 1 f.*
  FROM RESERVATION f WITH(NOLOCK)
  WHERE f.CUST_NUM = e.CUST_NUM AND f.RESERVE_FLAG = 'O' AND f.RESERVE_STATE <> 'C'
  ORDER BY f.RESERVE_NUM DESC
) opRsv
OUTER APPLY (
  SELECT CASE WHEN LEN(LTRIM(RTRIM(ISNULL(opRsv.RESERVE_NUM, '')))) >= 6
              THEN '20' + SUBSTRING(LTRIM(RTRIM(opRsv.RESERVE_NUM)), 1, 2)
                 + '-' + SUBSTRING(LTRIM(RTRIM(opRsv.RESERVE_NUM)), 3, 2)
                 + '-' + SUBSTRING(LTRIM(RTRIM(opRsv.RESERVE_NUM)), 5, 2)
              ELSE '' END AS regDate
) opReg
OUTER APPLY (
  SELECT TOP 1
    ISNULL(m2.category01_name, '') AS motiveL,
    ISNULL(m2.category02_name, '') AS motiveM,
    ISNULL(m2.category03_name, '') AS motiveS,
    ISNULL(NULLIF(m1.comments, ''), ISNULL(m2.name, '')) AS motiveMemo,
    ISNULL(CONVERT(varchar(10), m2.[section]), '') AS section
  FROM MOTIVE_NEW02 m2 WITH(NOLOCK)
  LEFT JOIN MOTIVE_NEW01 m1 WITH(NOLOCK) ON m2.fkey = m1.pkey
  WHERE m2.cust_num = e.CUST_NUM AND m2.Idx = '1'
  ORDER BY m2.pkey DESC
) motive
LEFT JOIN (
  SELECT TMP.PRC_CUSNUM, SUM(TMP.RECEIPT) AS RECEIPT
  FROM (
    SELECT A.CUST_NUM AS PRC_CUSNUM,
           SUM(ISNULL(A.COST_PRICE, 0)) AS RECEIPT
    FROM COSTPRICE A WITH(NOLOCK)
    LEFT JOIN (
      SELECT CstCusNum, CstSeq
      FROM CSTPRCSUB WITH(NOLOCK)
      GROUP BY CstCusNum, CstSeq
    ) B ON A.CUST_NUM = B.CstCusNum AND A.SEQ = B.CstSeq
    LEFT JOIN (
      SELECT PrcCusNum, PrcSeq
      FROM PRCITMLST WITH(NOLOCK)
      GROUP BY PrcCusNum, PrcSeq
    ) C ON A.CUST_NUM = C.PrcCusNum AND A.SEQ = C.PrcSeq
    WHERE B.CstCusNum IS NULL
      AND C.PrcCusNum IS NULL
      AND A.COST_FLAG IN ('O','I','H')
      AND UPPER(ISNULL(A.PayStt, '')) <> 'A'
    GROUP BY A.CUST_NUM

    UNION ALL

    SELECT B.CstCusNum AS PRC_CUSNUM,
           SUM(ISNULL(B.CstPrc, 0)) AS RECEIPT
    FROM COSTPRICE A WITH(NOLOCK)
    JOIN CSTPRCSUB B WITH(NOLOCK)
      ON A.CUST_NUM = B.CstCusNum
     AND A.SEQ = B.CstSeq
     AND A.COST_FLAG IN ('O','I','H')
     AND B.CstCnlYnN = 'N'
     AND B.CstPayTyp = 'N'
    LEFT JOIN PRCITMLST C WITH(NOLOCK)
      ON B.CstCusNum = C.PrcCusNum
     AND B.CstSeq = C.PrcSeq
    WHERE C.PrcCusNum IS NULL
      AND UPPER(ISNULL(A.PayStt, '')) <> 'A'
    GROUP BY B.CstCusNum

    UNION ALL

    SELECT T2.CUST_NUM AS PRC_CUSNUM,
           SUM(CASE D.PrcCnlYnN WHEN 'Y' THEN 0 ELSE ISNULL(D.PrcDtlPrc, 0) END) AS RECEIPT
    FROM (
      SELECT DISTINCT A.CUST_NUM, A.SEQ
      FROM COSTPRICE A WITH(NOLOCK)
      WHERE ISNULL(A.COST_FLAG, '') = ''
        AND UPPER(ISNULL(A.PayStt, '')) <> 'A'
    ) T2
    JOIN PRCITMLST C WITH(NOLOCK)
      ON T2.CUST_NUM = C.PrcCusNum
     AND T2.SEQ = C.PrcSeq
     AND C.PrcCod IN ('O','I','H')
    JOIN PrcItmDtl D WITH(NOLOCK)
      ON T2.CUST_NUM = D.PrcCusNum
     AND T2.SEQ = D.PrcSeq
     AND D.PrcCod IN ('O','I','H')
    GROUP BY T2.CUST_NUM
  ) TMP
  GROUP BY TMP.PRC_CUSNUM
) pay ON e.CUST_NUM = pay.PRC_CUSNUM
     AND e.CUST_NUM <> '9999999999999'
WHERE e.EXAM_DATE >= :from AND e.EXAM_DATE <= :to
  AND NOT (
    ISNULL(cu.CUST_NAME, '') LIKE N'%테스트%'
    OR LOWER(ISNULL(cu.CUST_NAME, '')) LIKE '%test%'
  )
  AND NOT (
    EXISTS (
      SELECT 1
      FROM Cataract_Exam ce WITH(NOLOCK)
      WHERE ce.CUST_NUM = e.CUST_NUM
        AND ce.EXAM_DATE = e.EXAM_DATE
    )
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.RIGHT01, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.LEFT01, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.RIGHT02, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.LEFT02, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.RIGHT03, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.LEFT03, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.RIGHT04, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.LEFT04, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.RIGHT05, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.LEFT05, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.RIGHT06, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.LEFT06, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.RIGHT07, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.LEFT07, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.RIGHT08, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.LEFT08, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.RIGHT09, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.LEFT09, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.RIGHT10, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.LEFT10, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.RIGHT11, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.LEFT11, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.RIGHT12, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.LEFT12, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.RIGHT13, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.LEFT13, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.RIGHT14, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.LEFT14, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.RIGHT15, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.LEFT15, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.RIGHT16, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.LEFT16, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.RIGHT17, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.LEFT17, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.RIGHT18, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.LEFT18, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.RIGHT19, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.LEFT19, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.RIGHT20, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.LEFT20, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.RIGHT21, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.LEFT21, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.RIGHT22, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.LEFT22, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.RIGHT23, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.LEFT23, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.RIGHT24, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.LEFT24, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.RIGHT25, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.LEFT25, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.RIGHT26, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.LEFT26, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.RIGHT27, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.LEFT27, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.RIGHT28, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.LEFT28, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.RIGHT29, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.LEFT29, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.RIGHT30, ''))), '') IS NULL
    AND NULLIF(LTRIM(RTRIM(ISNULL(e.LEFT30, ''))), '') IS NULL
  )
ORDER BY e.EXAM_DATE, rsv.RESERVE_DATE, rsv.START_TIME, cu.CUST_NAME
```

> **백내장 그림자 행 제외** `WHERE NOT (...)`: 같은 고객·검사일에 `Cataract_Exam` 본행이 있고 일반 `EXAM`의 `RIGHT01~30/LEFT01~30` 측정값이 **전부** 공백이면 제외(백내장 검사가 남긴 빈 일반검사 행 걸러냄). 위 SQL은 [ExamListRepository.java](../../backend/src/main/java/com/bviit/analytics/repository/exam/ExamListRepository.java) `findExamList`와 동일한 전문.

## 데이터 소스

### 원본 기본 구조

```sql
FROM EXAM A
  INNER JOIN CUSTOM B        ON A.CUST_NUM = B.CUST_NUM
  LEFT  JOIN OPERATIONDATA C ON A.CUST_NUM = C.CUST_NUM
  LEFT  JOIN EMPLOYEE D      ON B.MY_COUNSELOR   = D.EMP_NUM
  LEFT  JOIN EMPLOYEE E      ON B.MY_DOCTOR      = E.EMP_NUM
WHERE A.EXAM_DATE BETWEEN @start AND @end
  AND (B.MY_DOCTOR = @doc OR @doc = '-1')
  AND B.MY_COUNSELOR = @userId
```

원본은 상담사 본인 데이터 중심. 대시보드는 전체 현황 목적이라 상담사/상담의 조건 제거함.

### 확장 SQL 추가 조인

- 검사예약: 검사일과 같은 날의 최신 `RESERVATION.RESERVE_NUM`
  - `RESERVE_FLAG='M'`
  - `RESERVE_DATE = EXAM.EXAM_DATE`
  - `RESERVE_STATE IN ('I','H')`
  - `RESERVE_STATE<>'C'`
- 수술예약: 고객별 최신 `RESERVATION.RESERVE_NUM`
  - `RESERVE_FLAG='O'`
  - `RESERVE_STATE<>'C'`
- 진료구분: `MEDICAL_SUB_CFG.SUB_NAME`
  - `RESERVE_FLAG + RESERVE_JINRYO` 매핑
- 수납금액: `COSTPRICE`/`CSTPRCSUB`/`PRCITMLST`/`PrcItmDtl` 집계 `RECEIPT`
- 내원동기: `MOTIVE_NEW02` + `MOTIVE_NEW01`
- 보험사: `EtcCfg`
- 국적: `NationCustom_CFG`

### 백내장 분기

원본 계열은 수술구분에 따라 시력교정/백내장 `UNION` 가능.

| 시력교정 | 백내장 |
|---|---|
| `EXAM` | `Cataract_Exam` |
| `OPERATIONDATA` | `Cataract_Operationdata` |
| `OP_POSSIBLE_YN` | `Impossible` |

현재 API는 시력교정 `EXAM` 중심. 단, 백내장 본행이 있는 일반 `EXAM` 빈 그림자 행은 제외함.

## 컬럼 매핑

범례: `✓` 쿼리/실DB 확인 완료

| # | 엑셀 헤더 | 소스 | 비고 | 상태 |
|---:|---|---|---|:---:|
| 1 | 고객번호 | `CUSTOM.CUST_NUM` | PK | ✓ |
| 2 | 고객명 | `CUSTOM.CUST_NAME` | | ✓ |
| 3 | 고객명(영) | `CUSTOM.CUST_ENAME` | | ✓ |
| 4 | 검사일 | `EXAM.EXAM_DATE` | `char(10)` | ✓ |
| 5 | 검사예약진료구분 | `MEDICAL_SUB_CFG.SUB_NAME` ← 최신 검사예약 | 예: 검사OP | ✓ |
| 6 | 검사예약등록일 | 검사예약 `RESERVE_NUM` 앞 6자리 | `YYMMDD` → `YYYY-MM-DD` | ✓ |
| 7~36 | 검사정보1~30 | `EXAM.RIGHT01/LEFT01` ~ `RIGHT15/LEFT15` | 우/좌 15쌍을 30칸으로 출력 | ✓ |
| 37 | 검사특이사항 | `EXAM.EXAM_MEMO` | | ✓ |
| 38 | 견적가 | `EXAM.OP_ESTIMATE` | 텍스트 | ✓ |
| 39 | 수술가율 | `EXAM.ExamPercent` | | ✓ |
| 40 | 검사예약시간 | `RESERVATION.START_TIME` | 최신 검사예약 | ✓ |
| 41 | 적절한수술방법R | `EXAM.OPERATIONR` | | ✓ |
| 42 | 적절한수술방법L | `EXAM.OPERATIONL` | | ✓ |
| 43 | 수술예약등록일 | 수술예약 `RESERVE_NUM` 앞 6자리 | `YYMMDD` → `YYYY-MM-DD` | ✓ |
| 44 | 수술예약일 | `RESERVATION.RESERVE_DATE` | `RESERVE_FLAG='O'` | ✓ |
| 45 | 수술일 | `OPERATIONDATA.OPERATION_DATE` | 백내장: R/L_DATE max | ✓ |
| 46 | 수술방법R | `OPERATIONDATA.OPERATIONR` | | ✓ |
| 47 | 수술방법L | `OPERATIONDATA.OPERATIONL` | | ✓ |
| 48 | 집도의 | `OPERATIONDATA.OPERATION_DOC` → `EMPLOYEE.EMP_NAME` | | ✓ |
| 49 | 수납금액 | 결제 집계 `RECEIPT` | `EXAM.OP_UnitPrice` 아님 | ✓ |
| 50 | 상담사 | `CUSTOM.MY_COUNSELOR` → `EMPLOYEE.EMP_NAME` | | ✓ |
| 51 | 상담의 | `CUSTOM.MY_DOCTOR` → `EMPLOYEE.EMP_NAME` | | ✓ |
| 52 | 주민번호 | `CUSTOM.JUMIN_NUM` | API는 보안상 빈 값 | ✓ |
| 53 | 생일 | `CUSTOM.BIRTH_DAY` | | ✓ |
| 54 | 양/음 | `CUSTOM.LUNAR_YN` | `Y`=음, 그 외 양 | ✓ |
| 55 | 집전화 | `CUSTOM.CALL_NUM1` | | ✓ |
| 56 | 휴대전화 | `CUSTOM.CALL_NUM2` | | ✓ |
| 57 | 이메일 | `CUSTOM.EMAIL` | | ✓ |
| 58 | 우편번호 | `CUSTOM.ZIP_CODE` | | ✓ |
| 59 | 주소1 | `CUSTOM.ADDR1` | | ✓ |
| 60 | 주소2 | `CUSTOM.ADDR2` | | ✓ |
| 61 | 고객메모 | `CUSTOM.ETC` | | ✓ |
| 62 | 검사중단구분 | `EXAM.STOP_YN` | | ✓ |
| 63 | 수술불가구분 | `EXAM.OP_POSSIBLE_YN` | 백내장: `Impossible` | ✓ |
| 64 | 예약경로 | `RESERVATION.RESERVE_PATH` | 최신 검사예약 | ✓ |
| 65 | 섹션 | `MOTIVE_NEW02.section` | `Idx='1'` | ✓ |
| 66 | 내원동기(대) | `MOTIVE_NEW02.category01_name` | | ✓ |
| 67 | 내원동기(중) | `MOTIVE_NEW02.category02_name` | | ✓ |
| 68 | 내원동기(세) | `MOTIVE_NEW02.category03_name` | | ✓ |
| 69 | 내원동기메모 | `MOTIVE_NEW01.comments` fallback `MOTIVE_NEW02.name` | | ✓ |
| 70 | 검안사 | `CUSTOM.MY_OPTOMETRIST` → `EMPLOYEE.EMP_NAME` | 고객 담당 검안사 기준 | ✓ |
| 71 | 취소사유 | `EXAM.CANCEL_CD` | | ✓ |
| 72 | 취소사유메모 | `EXAM.CANCEL_REASON` fallback `CANCEL_CFG.CANCEL_REASON` | | ✓ |
| 73 | 등급 | `CUSTOM.LEVEL` | | ✓ |
| 74 | 직업 | `CUSTOM.JOB` | | ✓ |
| 75 | 만나이 | `CUSTOM.BIRTH_DAY` 파생 | 프론트 계산 가능 | ✓ |
| 76 | 최근내원일 | `CUSTOM.LAST_DAY` | | ✓ |
| 77 | 보험사 | `EtcCfg.EtcCodNam` | `EtcBseCod='InsuranceCfg'` | ✓ |
| 78 | 국적 | `NationCustom_CFG.Nation_CodeName` | | ✓ |

## 검사정보1~30

엑셀 `검사정보N` 구조:

- `EXAM.RIGHT01/LEFT01`부터 `RIGHT15/LEFT15`까지 사용
- 우/좌 15쌍을 엑셀 30칸으로 펼친 구조
- `RIGHT16~30/LEFT16~30`은 DB에 존재하나 원본 엑셀 30칸과 1:1 대응 아님

| 검사정보 | 우안 | 좌안 | 샘플 의미 |
|---:|---|---|---|
| 1 | `RIGHT01` | `LEFT01` | 구면(Sph) |
| 2 | `RIGHT02` | `LEFT02` | 난시(Cyl) |
| 3 | `RIGHT03` | `LEFT03` | 축(Axis) |
| 4 | `RIGHT04` | `LEFT04` | 시력 |
| 5~7 | `RIGHT05~07` | `LEFT05~07` | 시력/굴절 계열 추정 |
| 8 | `RIGHT08` | `LEFT08` | 안압(IOP) |
| 9 | `RIGHT09` | `LEFT09` | 각막두께(CCT) |
| 10~12 | `RIGHT10~12` | `LEFT10~12` | 검사 장비/폼 설정 의존 |
| 13 | `RIGHT13` | `LEFT13` | 전방깊이(ACD) 추정 |
| 14 | `RIGHT14` | `LEFT14` | 각막직경(WTW) 추정 |
| 15 | `RIGHT15` | `LEFT15` | 검사 장비/폼 설정 의존 |

> RIGHT/LEFT 인덱스 고정 라벨 없음.  
> 위 의미는 샘플 기반 추정. 정확한 항목명 필요 시 검사입력 화면/장비 매핑표 별도 확인 필요.

## 구현

| 항목 | 내용 |
|---|---|
| Repository | [ExamListRepository.java](../../backend/src/main/java/com/bviit/analytics/repository/exam/ExamListRepository.java) |
| Controller | [ExamListController.java](../../backend/src/main/java/com/bviit/analytics/controller/exam/ExamListController.java) |
| Frontend | [ExamListPage.tsx](../../frontend/src/pages/ExamListPage.tsx) |
| API client | [api/examList.ts](../../frontend/src/api/examList.ts) |
| Profile | `mssql` |
| Response | `ApiResponse<List<Map<String,Object>>>` |

구현 메모:

- DTO 없이 `Map` 반환. SQL alias = JSON key = 프론트 Zod 필드
- 프론트 계약상 전 컬럼 문자열 필수
- 주민번호는 `jumin=''` 고정
- 견적가/수납금액도 문자열로 내려줌
- 금액은 SoftCRM 결제 집계 `RECEIPT` 기준

## 검증 결과

| 기준 자료 | 기간 | 기준 건수 | API 건수 | 추가 | 누락 | 진료구분 불일치 |
|---|---:|---:|---:|---:|---:|---:|
| 1월 레거시 고객번호 명단 | 2026-01 | 1,893 | 1,893 | 0 | 0 | - |
| 2월 레거시 고객번호 명단 | 2026-02 | 1,612 | 1,611 | 0 | 1 | - |
| `검사자리스트2026_04월.xlsx` | 2026-04 | 826 | 826 | 0 | 0 | 0 |
| 5월 레거시 고객번호 명단 | 2026-05 | 923 | 923 | 0 | 0 | - |

검증 중 확인한 보정:

- `2228838 / 위명용 / 2026-04-04`
  - `Cataract_Exam` 본행 존재
  - 일반 `EXAM` 측정값 전부 공백
  - 백내장 검사 그림자 행으로 판단, 일반 검사자 리스트에서 제외
- `9000005 / 테스트환자_0526`
  - 고객명/예약번호/검사메모에 테스트 표시 존재
  - 테스트 데이터로 판단, 출력 제외
- `장용테스트` 포함 실제 고객 3건
  - 정상 임상 메모로 판단
  - 검사메모 필터를 `테스트%`/`test%` 시작 조건으로 좁혀 오탐 방지함
- `2225908 / 최영두`, `2226759 / 박소연`
  - 검사메모의 `모노비전TEST` 문구 때문에 테스트 데이터로 오탐됨
  - 정상 검사로 판단, 검사메모/예약번호의 `test` 제외 조건 제거함
- `2076713 / 외래test`
  - 2월 레거시에는 포함됨
  - 고객명 영문 `test` 제외 규칙에 따라 현재 API에서는 제외됨

## 주의사항

- 원본 SQL은 문자열 결합 방식. 이식 시 파라미터 바인딩 필수
- 원본 `FrmCounselExamList` 구버전은 25컬럼. 운영 엑셀 기준은 확장 75/78컬럼 SQL
- 원본은 상담사 본인 데이터 필터 존재. java_dashboard는 전체 조회 기준
- 백내장 리스트까지 같은 API에서 보여야 하면 `Cataract_Exam`/`Cataract_Operationdata` `UNION` 설계 필요
- 루트 엑셀은 75열, 원본 정의는 78열 계열. 엑셀 export 옵션에 따라 최근내원일/보험사/국적 누락 가능
- `CUSTOM.MY_OPTOMETRIST`는 고객 담당 검안사 기준. 검사일 실제 수행자와 다를 수 있음
- 주민번호 복호화는 보안/성능 리스크 큼. API 출력 제외 유지 권장

## 관련 문서

- [지표정의.md](지표정의.md) — 검사/수술/예약 집계 정의
- [tables/EXAM.md](tables/EXAM.md) — 일반 검사 테이블
- [tables/CUSTOM.md](tables/CUSTOM.md) — 고객 마스터
- [tables/OPERATIONDATA.md](tables/OPERATIONDATA.md) — 수술 데이터
- [tables/RESERVATION.md](tables/RESERVATION.md) — 예약 테이블
- [tables/EMPLOYEE.md](tables/EMPLOYEE.md) — 직원 마스터
