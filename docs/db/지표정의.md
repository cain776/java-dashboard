# 지표 정의 (검사 · 수술 · 예약)

> 대시보드 집계 지표의 **공식 정의서**. 병원 자체 집계와 수치가 어긋날 때 이 문서가 기준이다.
> 모든 SQL은 MSSQL(SOFTCRM) 기준, READ-ONLY, 대용량 테이블은 `WITH(NOLOCK)` + 날짜 범위 필수.
> 최종 갱신: 2026-06-19

## 정의 상태

| 지표 | 상태 | 비고 |
|------|------|------|
| 검사건수 | ✅ 정의됨 | 시력교정 · 백내장 · 드림렌즈 3종 |
| 검사수(전체) | ✅ 정의됨 | `검사건수` 메뉴 화면 — EXAM 행수 + Cataract_Exam 세션수 raw 합산 (§1.9) |
| 검사자 직업 구성 | ✅ 정의됨 | 검사수 모집단을 직장인/학생/기타로 분류 — `CUSTOM.JOB` 롤업 (§1.10) |
| 검사자 종합표(35칼럼) | 🟡 2024~25 고정 + 2026 부분 | 9블록 35칼럼 — 2024~25 레거시 고정, 2026은 산출 가능한 칼럼 반영, 백내장 중단제외·일부 세부 정의만 미정 (§6) |
| 수술건수 | ✅ 정의됨 | 실제 수술일 기준 · 시력교정 검수값 일부 고정 |
| 일별 산출물 | ✅ 운영원칙 정의 | 2026년 이후 일별 저장, 월/주/기간은 합산 |
| 외래수 | ✅ 정의됨 | 2024~2025 레거시 고정, 2026~ DB(`F`+`I/H` 행수) (§4) |
| 예약 종합(콜·온라인) | ✅ 정의됨 | 3탭(종합/온라인/콜). 2024~25 레거시 고정, 2026~ DB(등록일 기준·콜 B2B제외·종합=콜+온라인) (§5.1) |
| 예약건수 | ⬜ 미정의 | TODO (§5.2) |

---

## 1. 검사건수

검사는 **시력교정 · 백내장 · 드림렌즈** 3종으로 구분한다. **각 카테고리는 소스·집계단위가 서로 다르다.**

### 1.1 개요

| 카테고리 | 소스 테이블 | 분류 조건 | 집계 단위 | 대표 검증값 |
|---|---|---|---|---|
| **시력교정검사** | `EXAM` (검사자 리스트) | `EXAM_DATE` 기준, **드림렌즈 분류 제외** (측정값 무관) | **행/사람** | 2026-04 = 809 |
| **백내장검사** | `Cataract_Exam` | 같은 날 백내장 검사예약 `H` 내원 + 추천 수술방법 입력 | **눈** (좌/우 각각) | 2026-01~04 = 96 / 66 / 64 / 58 |
| **드림렌즈검사** | `EXAM` (검사자 리스트) | 같은 검사일 렌즈센터 `D`만 있고 검사 `M`이 없는 행 | **행/사람** | 2026-04 = 17 |

> ⚠️ **단위가 달라 단순 합산은 의미가 다르다** (시력교정·드림렌즈=사람, 백내장=눈). 통합 "총 검사건수"가 필요하면 단위를 통일하는 별도 합의가 필요하다.

### 1.2 공통 규칙

**검사결과 테이블 기반** (시력교정=`EXAM`, 백내장=`Cataract_Exam`)
- 날짜 기준: `EXAM_DATE` (char(10), `YYYY-MM-DD`) — 실제 검사일
- **백내장은 "추천 수술방법 존재"(`OPERATIONR`/`OPERATIONL`)로 유효 눈 판정**. 같은 날 백내장 검사예약(`RESERVE_FLAG='H'`) 내원 상태가 있어야 한다.
- **시력교정은 측정값 무관** — 병원 검사자 리스트 행수 기준이라 측정값 미입력 검사도 포함.
- 더 엄격한 "완료" 기준이 필요하면 `STOP_YN='Y'`(중단)·`CANCEL_CD`(취소)를 추가 제외.

**EXAM + RESERVATION 분류 기반** (드림렌즈)
- 소스: `EXAM WITH(NOLOCK)` 기준 행을 `RESERVATION` 렌즈센터 `D` 예약으로 분류
- 상태: `RESERVE_STATE IN ('I','H')` — 예약(Y)·취소(C) 제외. 이 병원은 'I'에서 멈추므로 사실상 **"내원(체크인)"**.
- 날짜 기준: `RESERVE_DATE`
- 정기검진(FU)은 제외.

---

### 1.3 시력교정검사

**정의**: 해당 기간에 시력교정 검사를 실제 받은 **사람 수**. 소스는 검사결과 테이블 `EXAM` (병원 "검사자 리스트(상담사별)" 화면과 동일 소스). **EXAM에 섞인 드림렌즈는 제외.**

| 항목 | 값 |
|------|-----|
| 소스 | **`EXAM`** (시력교정 검사결과, PK=`CUST_NUM`) |
| 날짜 기준 | `EXAM_DATE` (char(10), `YYYY-MM-DD`) |
| 측정값 필터 | **없음** — 병원 검사자 리스트 행수 기준(측정값 미입력 검사도 포함). 엄격 "실시"분만 필요하면 `RIGHT01`/`LEFT01` 존재 추가 |
| 드림렌즈 제외 | 같은 날 렌즈센터(`RESERVE_FLAG='D'`) 예약만 있고 시력교정(`'M'`) 예약 없는 EXAM 배제 |
| 집계 단위 | **사람** = `COUNT(DISTINCT CUST_NUM)` (EXAM은 고객당 1행이라 행수와 동일) |

> **소스가 RESERVATION이 아니라 `EXAM`인 이유**: 병원 공식 "검사자 리스트(상담사별)" 화면(`FrmCounselExamList`)이 시력교정을 `EXAM`+`EXAM_DATE`로 집계한다. "실제 검사 실시" 기준이며 백내장(`Cataract_Exam`)과 동일 구조다. RESERVATION(flag M) 예약기반 집계는 검사 실시와 불일치(과거 정의 843/805 → 본 정의로 교체).
> **드림렌즈 혼입 주의**: `EXAM`에는 검사종류 구분 컬럼이 없어 드림렌즈(CRT/각막굴절교정) 검사가 섞여 있다. 같은 날 렌즈센터(`D`) 예약 기준으로 배제한다.

```sql
SELECT SUBSTRING(e.EXAM_DATE,1,7) AS ym,
       COUNT(DISTINCT e.CUST_NUM) AS visionCorrection
FROM EXAM e WITH(NOLOCK)
WHERE e.EXAM_DATE >= :from AND e.EXAM_DATE <= :to
  AND NOT (                                    -- 드림렌즈 배제
        EXISTS (SELECT 1 FROM RESERVATION rd WITH(NOLOCK)
                WHERE rd.CUST_NUM = e.CUST_NUM AND rd.RESERVE_DATE = e.EXAM_DATE
                  AND rd.RESERVE_STATE IN ('I','H') AND rd.RESERVE_FLAG = 'D')
    AND NOT EXISTS (SELECT 1 FROM RESERVATION rm WITH(NOLOCK)
                WHERE rm.CUST_NUM = e.CUST_NUM AND rm.RESERVE_DATE = e.EXAM_DATE
                  AND rm.RESERVE_STATE IN ('I','H') AND rm.RESERVE_FLAG = 'M')
  )
GROUP BY SUBSTRING(e.EXAM_DATE,1,7)
ORDER BY ym
```

- 보조(엄격 "실시" 기준): 위 조건 + 측정값 존재(`RIGHT01`/`LEFT01`) → 측정 완료분만 02=1585 / 03=850 / 04=799 (데스크는 미입력도 세므로 기본 정의는 측정값 무관).
- **검증 (병원 수기 대조)**: 02=1600(병원 1604) / 03=854(853) / 04=809(807) / 05=900. 04월은 병원 엑셀 826행 = EXAM 4월 전체와 1:1 대조로 확정. 드림렌즈 미제외 시 02=1611/03=873/04=826.

---

### 1.4 백내장검사

**정의**: 해당 기간에 백내장 검사를 받은 뒤 **추천 수술방법이 기입된 눈(좌/우)** 을 집계. 양안 추천 = 2건, 단안 추천 = 1건.

| 항목 | 값 |
|------|-----|
| 소스 | **`Cataract_Exam`** (실제 백내장 검사 상세, `CUST_NUM`+`SEQ`) |
| 날짜 기준 | `EXAM_DATE` (char(10), `YYYY-MM-DD`) |
| 예약 보조조건 | 같은 고객·같은 날짜 `RESERVATION.RESERVE_FLAG='H'`, `RESERVE_STATE IN ('I','H')` 존재 |
| 제외 | `Stop_YN='Y'`(중단), `Cancel_CD` 채워짐(취소), 테스트 고객명 |
| 눈 판정 | 우안 = `OPERATIONR` 채워짐, 좌안 = `OPERATIONL` 채워짐 |
| 집계 단위 | **눈 (좌/우 각각 1건)** = `SUM(OPERATIONR 채움) + SUM(OPERATIONL 채움)` |

> **측정값(`RIGHT01`/`LEFT01`) 기준을 사용하지 않음.** 레거시 백내장 검사건수와 대조한 결과, 단순 측정 눈 수(2026-04=214안)가 아니라 **백내장 수술 추천을 받은 눈 수**가 운영 지표에 맞는다.
> 2024~2025년은 레거시 확정값을 고정 사용하고, 2026년 이후는 아래 기준으로 계산한다.

```sql
SELECT SUBSTRING(ce.EXAM_DATE,1,7) AS ym,
       SUM(CASE WHEN NULLIF(LTRIM(RTRIM(ISNULL(ce.OPERATIONR,''))), '') IS NOT NULL THEN 1 ELSE 0 END)
     + SUM(CASE WHEN NULLIF(LTRIM(RTRIM(ISNULL(ce.OPERATIONL,''))), '') IS NOT NULL THEN 1 ELSE 0 END) AS cataract
FROM Cataract_Exam ce WITH(NOLOCK)
JOIN CUSTOM cu WITH(NOLOCK) ON ce.CUST_NUM = cu.CUST_NUM
WHERE ce.EXAM_DATE >= :from AND ce.EXAM_DATE <= :to
  AND (ce.Stop_YN IS NULL OR ce.Stop_YN <> 'Y')
  AND (ce.Cancel_CD IS NULL OR LTRIM(RTRIM(ce.Cancel_CD)) = '')
  AND NOT (ISNULL(cu.CUST_NAME,'') LIKE N'%테스트%' OR LOWER(ISNULL(cu.CUST_NAME,'')) LIKE '%test%')
  AND EXISTS (
    SELECT 1
    FROM RESERVATION r WITH(NOLOCK)
    WHERE r.CUST_NUM = ce.CUST_NUM
      AND r.RESERVE_DATE = ce.EXAM_DATE
      AND r.RESERVE_FLAG = 'H'
      AND r.RESERVE_STATE IN ('I','H')
  )
GROUP BY SUBSTRING(ce.EXAM_DATE,1,7)
ORDER BY ym
```

- 보조: 검사인원 = `COUNT(DISTINCT ce.CUST_NUM)`, 검사세션 = `COUNT(*)`, 측정 눈 수는 품질 점검용으로만 사용.
- **2026 검증값**: 01=96 / 02=66 / 03=64 / 04=58.
- **레거시 고정값**: 2024~2025는 화면 표의 숫자를 그대로 사용. 2026은 DB 계산값 사용.

---

### 1.4.1 백내장예약률

**정의**: 백내장 검사건수로 선별된 추천 눈(안) 중, 백내장 수술예약으로 전환된 비율.

| 항목 | 값 |
|------|-----|
| 화면명 | `백내장예약률 (수술예약건/검사자)` |
| 분모 | `백내장검사`와 동일한 추천 눈 수 (`Cataract_Exam.OPERATIONR/L`) |
| 분자 | 분모에 포함된 고객 중 취소 아님인 수술예약 `RESERVATION.RESERVE_FLAG='O'` 존재 |
| 날짜 기준 | 분모는 `Cataract_Exam.EXAM_DATE`, 수술예약은 검사자 리스트의 `수술예약등록일` 존재 여부와 동일하게 판정 |
| 집계 단위 | 분모는 **눈(안)**, 분자는 **고객/예약 건** |
| 2024~2025 | 레거시 예약률 표 숫자 고정 사용 |
| 2026 이후 | DB 계산값 사용 |

> **검사 예약등록일은 분자 아님.** 백내장 검사건수 분모는 이미 같은 날 백내장 검사예약(`H`) 내원을 보조조건으로 요구하므로, 검사예약 존재 여부를 분자로 쓰면 사실상 100%가 된다. 예약률 분자는 반드시 **수술예약(`O`)** 이어야 한다.
> `RESERVATION`에는 좌/우 눈 구분이 없으므로 분자는 눈 단위로 증폭하지 않고, 검사자 리스트의 `수술예약등록일` 존재 여부처럼 고객/예약 건 단위로 집계한다.

---

### 1.5 드림렌즈검사

**정의**: 해당 기간 드림렌즈/렌즈센터 검사 건수. 검사자 리스트와 같은 `EXAM` 기준에서 같은 날 렌즈센터 `D` 예약만 있고 검사 `M` 예약이 없는 행을 집계.

| 항목 | 값 |
|------|-----|
| 소스 | `EXAM` + `RESERVATION` 분류 |
| 진료유형 | `RESERVE_FLAG = 'D'` |
| 상태 | `RESERVE_STATE IN ('I','H')` |
| 세부유형 | 제한 없음 |
| 집계 단위 | **`COUNT(DISTINCT CUST_NUM)` (사람단위)** — 시력교정과 통일 |

> 2026-02만 보고 D/L 계열(`1`,`5`,`7`)로 좁히면 01·03·04월 시력교정 숫자가 더 흔들림. 현재는 원복하여 렌즈센터 `D` 전체를 드림렌즈 분류로 사용.

```sql
SELECT SUBSTRING(e.EXAM_DATE,1,7) AS ym,
       COUNT(*) AS dreamlensPersons
FROM EXAM e WITH(NOLOCK)
WHERE e.EXAM_DATE >= :from AND e.EXAM_DATE <= :to
  AND EXISTS (SELECT 1
              FROM RESERVATION rd WITH(NOLOCK)
              WHERE rd.CUST_NUM = e.CUST_NUM
                AND rd.RESERVE_DATE = e.EXAM_DATE
                AND rd.RESERVE_STATE IN ('I','H')
                AND rd.RESERVE_FLAG = 'D')
  AND NOT EXISTS (SELECT 1
                  FROM RESERVATION rm WITH(NOLOCK)
                  WHERE rm.CUST_NUM = e.CUST_NUM
                    AND rm.RESERVE_DATE = e.EXAM_DATE
                    AND rm.RESERVE_STATE IN ('I','H')
                    AND rm.RESERVE_FLAG = 'M')
GROUP BY SUBSTRING(e.EXAM_DATE,1,7)
ORDER BY ym
```

- **검증**: 02=11 / 03=19 / 04=17 / 05=23 명. 검사자 리스트 전체에서 분리한 값.

---

### 1.6 함정 (Gotchas)

- **카테고리별 소스·단위 상이**: 시력교정=`EXAM`/사람, 백내장=`Cataract_Exam`/추천 눈, 드림렌즈=`EXAM`+`RESERVATION` 분류/사람. 코드·차트에 소스/단위 라벨 명시 필수.
- **시력교정 소스 주의**: 예약(`RESERVATION` flag M)이 아니라 **검사결과(`EXAM`)** 기준. 예약기반은 병원 공식 수치와 어긋난다.
- **EXAM에 드림렌즈 혼입**: `EXAM`엔 검사종류 구분 컬럼이 없어 드림렌즈(CRT/각막굴절교정) 검사가 월 ~2%(11-21명) 섞인다. 같은 날 렌즈센터(`D`) 예약 기준으로 반드시 배제(미배제 873/826 → 배제 854/809).
- **측정값 필터는 시력교정 기본 정의에서 미적용**: 병원 데스크가 측정값 미입력 검사도 리스트에 세므로 행수 기준으로 맞춘다. 측정값 존재(`RIGHT01`/`LEFT01`) 조건은 엄격 "실시" 보조지표로만(측정분 1585/850/799). 백내장은 측정값이 아니라 추천 수술방법(`OPERATIONR/L`) 기준.
- **EXAM은 스냅샷(고객당 1행) → 과거 월 수치가 사후 감소**: 고객이 나중에 재검사하면 `EXAM_DATE`가 덮어써져 원래 달에서 빠진다. 실측 월 -3~-5명(~0.3%) 수준. 과거 월 수치는 조회 시점에 따라 미세하게 달라질 수 있음.
- **측정값 미입력 검사(월 ~12명)**: `EXAM_DATE`는 해당월인데 `RIGHT01`/`LEFT01`이 빈 검사. 병원 데스크와 본 정의(측정값 무관)는 포함, 엄격 "실시" 보조지표에선 제외.
- **백내장 단위 혼동 금지**: "추천 눈" 단위라 인원과 다를 수 있음. 시력교정·드림렌즈(사람)와 직접 비교 시 단위 라벨을 반드시 확인.
- **드림렌즈 분류 기준 주의**: 02월만 보면 D/L 계열만 분리하는 기준이 맞아 보이나, 01·03·04월은 렌즈센터 `D` 전체 분리가 레거시 시력교정 수치에 더 가깝다.
- **드림렌즈 상태값 `I` 단독**: `RESERVATION` `IN('I','H')`는 사실상 "내원(체크인)"이며, 최종 집계는 `EXAM` 행 존재를 함께 요구한다.
- **병원 수기집계와 ±오차 정상**: 병원 자체 수치(시력교정 03=853/04=807 등)는 수기/엑셀 집계라 시스템 정의와 1~2% 차이는 자연스럽다.

### 1.7 검증 이력 (2026년)

| 카테고리 | 단위 | 2026-01 | 2026-02 | 2026-03 | 2026-04 |
|---|---|--:|--:|--:|--:|
| 시력교정검사 | 사람 (EXAM, 드림렌즈 분류 제외) | 1,873 | 1,600 | 854 | 809 |
| 백내장검사 | 추천 눈 (좌/우) | 96 | 66 | 64 | 58 |
| 드림렌즈검사 | 사람 (EXAM, 렌즈센터 D) | 20 | 11 | 19 | 17 |

> 1·2월은 겨울방학 시즌이라 시력교정이 3~5월의 약 2배.

**시력교정 ↔ 병원 자체 수기집계 대조**

| 구성 요소 | 2026-02 | 2026-03 | 2026-04 |
|---|--:|--:|--:|
| API 기준 EXAM 전체 | 1,611 | 873 | 826 |
| − 렌즈센터 D 드림렌즈 배제 | −11 | −19 | −17 |
| **= 정의값** (렌즈센터 D 제외, 측정값 무관) | **1,600** | **854** | **809** |
| **병원 수기** | **1,604** | **853** | **807** |
| 잔차 | -4 | +1 | +2 |

> 잔차 ±4(≤0.4%)는 **EXAM 스냅샷 덮어쓰기**(과거월일수록 미세 과소, 월 +3~5)와 수기집계 노이즈로 설명된다. 04월은 병원 엑셀(826행) = EXAM 4월 전체와 1:1 대조로 확정. 참고로 측정값까지 요구하는 엄격 "실시" 기준은 1585/850/799(데스크 대비 −1%).

### 1.8 코드 반영 상태

- [ExaminationStatsRepository.java](../../backend/src/main/java/com/bviit/analytics/exam/repository/ExaminationStatsRepository.java):
  - 시력교정: `EXAM` + 드림렌즈 배제, **측정값 필터 제거 완료** (`findVisionCorrectionMonthly`) ✅
  - 드림렌즈: 검사자 리스트와 같은 `EXAM` 기준 (`findDreamlensMonthly`) ✅
  - 백내장: `Cataract_Exam` 추천 수술방법 입력 눈 + 같은 날 `H` 내원 기준 (`findCataractMonthly`) ✅
- API 응답(`ExaminationMonthlyItem`)은 `visionCorrection`, `dreamlens`, `cataract`, `examTotal`, `total` 사용.
- 실DB 검증: 02~05월 시력교정 **1600/854/809/900**, 드림렌즈 **11/19/17/23**. 두 항목 합계는 검사자 리스트 월별 건수와 일치.
- 백내장 검증: 2026-01~04월 추천 눈 **96/66/64/58**. 04월 레거시 58과 일치.
- 2026-02 D/L 계열만 드림렌즈로 좁히는 안은 01·03·04월 시력교정 수치가 더 흔들려 원복.
- `/stats/examination`은 기본 JSON 캐시 사용. 캐시 파일이 없으면 DB 쿼리 후 `.cache/stats/examination-monthly-v7-*.json`에 저장.
- 2024~2025년 `시력교정검사`(`visionCorrection`)는 레거시 출력 확정값을 사용. DB 재집계로 업데이트하지 않음.
- 2024~2025년 `백내장검사`(`cataract`)는 레거시 출력 확정값을 사용. DB 재집계로 업데이트하지 않음.
- 2026년 이후 `백내장검사`(`cataract`)는 DB 계산값 사용. 현재 확정값: 2026-01=96 / 02=66 / 03=64 / 04=58.
- 화면 `전체`(`examTotal`)는 항상 `시력교정검사 + 드림렌즈검사 + 백내장검사`.

---

### 1.9 검사건수 메뉴 — `검사수` / `원데이 검사`

> 메뉴 구조: **`검사건수`** 메뉴(`/stats/procedure-exam`)는 `검사수`와 `원데이 검사` 탭으로 구성. **`시술별`** 메뉴는 §1.3~1.5 카테고리별(시력교정/드림렌즈/백내장 탭) 화면.

#### 1.9.1 검사수

**정의**: 레거시 "검사수" 차트(병원 엑셀)를 재현한 **전체 검사 건수**.
`검사수 = EXAM 검사자 행수 + Cataract_Exam 검사 세션수`. **둘 다 raw `COUNT(*)`** — 테스트/취소/드림렌즈/눈 단위 필터를 적용하지 않는다.

| 구성 | 소스 | 집계 |
|---|---|---|
| EXAM 행수 | `EXAM` | `COUNT(*)` (시력교정 + 드림렌즈 = 검사자 리스트 전체 행) |
| Cataract_Exam 세션수 | `Cataract_Exam` | `COUNT(*)` (모든 검사행, 눈 단위 아님) |

```sql
SELECT t.yr, t.mo, SUM(t.cnt) AS cnt
FROM (
    SELECT CAST(SUBSTRING(EXAM_DATE,1,4) AS INT) AS yr, CAST(SUBSTRING(EXAM_DATE,6,2) AS INT) AS mo, COUNT(*) AS cnt
    FROM EXAM WITH(NOLOCK) WHERE EXAM_DATE >= :from AND EXAM_DATE <= :to
    GROUP BY CAST(SUBSTRING(EXAM_DATE,1,4) AS INT), CAST(SUBSTRING(EXAM_DATE,6,2) AS INT)
    UNION ALL
    SELECT CAST(SUBSTRING(EXAM_DATE,1,4) AS INT) AS yr, CAST(SUBSTRING(EXAM_DATE,6,2) AS INT) AS mo, COUNT(*) AS cnt
    FROM Cataract_Exam WITH(NOLOCK) WHERE EXAM_DATE >= :from AND EXAM_DATE <= :to
    GROUP BY CAST(SUBSTRING(EXAM_DATE,1,4) AS INT), CAST(SUBSTRING(EXAM_DATE,6,2) AS INT)
) t
GROUP BY t.yr, t.mo ORDER BY t.yr, t.mo
```

> **§1 카테고리 정의와의 차이 (중요)**: §1의 화면 `전체`(`examTotal`)는 백내장을 **추천 눈(96)** 으로 세지만, 본 검사수는 백내장을 **Cataract_Exam raw 세션(145)** 으로 센다. 시력교정+드림렌즈 부분(EXAM 전체 행)은 양쪽 동일.
> 예) 2026-01: §1 `examTotal` = 1,873 + 20 + 96 = **1,989** vs 검사수 = 1,893 + 145 = **2,038** (차이 49 = 145 − 96).

**데이터 공급**
- **2024~2025**: 레거시 차트 확정값(업로드 데이터) 고정. DB 조회 안 함.
- **2026년 이후**: prod DB 계산값 라이브. EXAM 스냅샷 변동을 반영하려고 **캐시하지 않는다.**

**레거시 확정값 (월별, 1~12월)**

| 연도 | 01 | 02 | 03 | 04 | 05 | 06 | 07 | 08 | 09 | 10 | 11 | 12 |
|---:|--:|--:|--:|--:|--:|--:|--:|--:|--:|--:|--:|--:|
| 2024 | 2432 | 2035 | 1286 | 1140 | 1380 | 1388 | 1674 | 1728 | 1324 | 1258 | 1568 | 2234 |
| 2025 | 2436 | 2114 | 1177 | 1124 | 1256 | 1132 | 1291 | 1337 | 1190 | 1357 | 1085 | 1860 |

**2026 검증 (prod 실측, EXAM 행 + Cataract_Exam 세션)**

| 월 | EXAM 행 | Cataract 세션 | 검사수 | 차트값 | 차이 |
|---|--:|--:|--:|--:|--:|
| 01 | 1893 | 145 | **2038** | 2038 | 0 |
| 02 | 1612 | 100 | 1712 | 1721 | −9 |
| 03 | 873 | 128 | 1001 | 1007 | −6 |
| 04 | 826 | 110 | 936 | 932 | +4 |
| 05 | 922 | 132 | 1054 | 1062 | −8 |

> 01월은 정확 일치. 02~05월 ±4~9 차이는 **EXAM 스냅샷 덮어쓰기**(차트 생성 이후 과거 월 행수가 미세 변동) 때문이며 prod에서도 동일하게 나타난다. 정확 일치가 필요하면 2026도 확정값 고정으로 전환.

#### 1.9.1.1 검사수 안정화 검토 (EXAM 스냅샷 드리프트 — ExamCount 기각)

**문제**: `EXAM`은 고객당 1행(PK=`CUST_NUM`)인 **스냅샷**이라, 5월 검사자가 나중에(6월 등) 재검사하면 `EXAM_DATE`가 새 날짜로 덮여 **과거 월 검사수가 사후 감소**한다(드리프트, 월 −2~−11). 레거시 차트값(1062 등)도 결국 그 시점의 EXAM 스냅샷을 얼린 값이다.

**대안 검토 — 누적/이벤트성 소스로 재정의 시도 (2026-06 prod 전수 점검)**

| 테이블 | 상태 | 판정 |
|---|---|---|
| `EXAM` | 스냅샷(고객당 1행, 덮어씀), 현재 | 공식 소스. 드리프트 존재 |
| `ExamCount` | **append 로그**(고객당 다중, ~48만행), 2026-06까지 라이브 | 유일한 누적이나 아래 사유로 부적합 |
| `EXAM_FU` | 정기검진(follow-up) | 검사수 정의에서 제외 |
| `InspcLst_Exam` / `exam_dto` | 0행(미사용/빈 staging) | ✗ |
| `CHART_EXAM` | 2009~2010만 | ✗ |
| `명동_검사정보_일반` | ~2017 옛 export | ✗ |

**`ExamCount` 기각 사유** (3가지 환산 모두 레거시 초과):

| 환산 방식 | 01 | 02 | 03 | 04 | 05 | 비고 |
|---|--:|--:|--:|--:|--:|---|
| EXAM기준(현재) − 레거시 | 0 | −9 | −6 | +4 | −8 | 총 |오차| 27 |
| ExamCount 단순합집합 − 레거시 | +58 | +55 | +13 | +21 | +10 | 복수검사일·저장중복 과대 |
| ExamCount 고객dedup(2→1) − 레거시 | +58 | +55 | +13 | +21 | +10 | 동일(고객단위 union) |
| ExamCount **정밀 드리프트만** − 레거시 | +11 | −5 | −4 | +6 | −6 | 총 |오차| 32 (악화) |

- `ExamCount`는 **저장 이벤트마다 1행**(5월 raw 3,985 = 검사자 922 × 다중 저장)이고 **재검사·추가검사까지 포함**(넓음). 반대로 EXAM 일부는 누락(5월 distinct 911 < EXAM 922)이라 깔끔한 상위집합도 아님.
- **결정적 반례**: 01월은 드리프트 0(EXAM=레거시=2038)인데 ExamCount 복원은 +11~58을 더한다. 즉 **레거시 숫자 자체가 시점 스냅샷**이라, "완전 복원값"과는 정의가 달라 양쪽을 동시에 만족하는 값이 없다.
- **예약(RESERVATION) 기반**도 검토: 안정적이나 내원/예약 ≠ 검사 실시이며 과대(5월 M-내원 1,103 vs EXAM 922) → 과거 폐기 이력과 동일(§1.3).

**결론 / 방침**

- **검사수 공식 소스 = `EXAM`(+`Cataract_Exam`) 유지.** ExamCount로의 재정의는 정의 불일치로 기각.
- 드리프트 제거는 **월마감 freeze**(§3 일별 산출물 저장)로 한다 — 레거시가 한 것과 동일하게, 월이 닫히면 그 시점 검사수를 확정 저장. 이것이 유일하게 자기일관적이다.
- `ExamCount`는 측정값·추세 분석용 **보조 로그**로만 활용한다(카운트 소스 아님).

#### 1.9.2 원데이 검사

**정의**: 레거시 "원데이 검사" 차트를 재현하기 위한 검사OP 기반 검사 건수. 실제 같은 날 수술 여부가 아니라, 검사자 리스트(`EXAM`) 중 같은 날 `RESERVATION` 시력교정 검사예약 `M/5(검사OP)`로 내원한 건을 센다.

| 항목 | 값 |
|---|---|
| 소스 | `EXAM` + `RESERVATION` |
| 날짜 기준 | `EXAM.EXAM_DATE` |
| 원데이 판정 | 같은 고객·같은 날짜 `RESERVATION.RESERVE_FLAG='M'`, `RESERVE_JINRYO='5'`, `RESERVE_STATE IN ('I','H')` 존재 |
| 제외 | `EXAM.STOP_YN='Y'`, `EXAM.CANCEL_CD` 존재, 테스트 고객명/검사메모/예약번호 |
| 집계 단위 | `COUNT(*)` (EXAM 행/사람 단위) |

```sql
SELECT SUBSTRING(e.EXAM_DATE,1,7) AS ym,
       COUNT(*) AS oneDayExamCount
FROM EXAM e WITH(NOLOCK)
JOIN CUSTOM cu WITH(NOLOCK) ON cu.CUST_NUM = e.CUST_NUM
WHERE e.EXAM_DATE >= :from AND e.EXAM_DATE <= :to
  AND ISNULL(e.STOP_YN, '') <> 'Y'
  AND ISNULL(e.CANCEL_CD, '') = ''
  AND NOT (
    ISNULL(cu.CUST_NAME, '') LIKE N'%테스트%'
    OR LOWER(ISNULL(cu.CUST_NAME, '')) LIKE '%test%'
    OR ISNULL(e.EXAM_MEMO, '') LIKE N'%테스트%'
    OR LOWER(ISNULL(e.EXAM_MEMO, '')) LIKE '%test%'
  )
  AND EXISTS (
    SELECT 1
    FROM RESERVATION r WITH(NOLOCK)
    WHERE r.CUST_NUM = e.CUST_NUM
      AND r.RESERVE_DATE = e.EXAM_DATE
      AND r.RESERVE_FLAG = 'M'
      AND r.RESERVE_STATE IN ('I','H')
      AND r.RESERVE_JINRYO = '5'
      AND LOWER(ISNULL(r.RESERVE_NUM, '')) NOT LIKE '%test%'
      AND LOWER(ISNULL(r.RESERVE_NUM, '')) NOT LIKE '%kiosktest%'
  )
GROUP BY SUBSTRING(e.EXAM_DATE,1,7)
ORDER BY ym
```

**데이터 공급**
- **2024~2025**: 레거시 차트 확정값 고정. DB 조회값으로 대체하지 않음.
- **2026년 이후**: prod DB 계산값 라이브. 레거시 2026 1~4월과 총합 기준 거의 일치하므로 고정값 대신 운영 기준을 사용.

**레거시 확정값 (월별, 1~12월)**

| 연도 | 01 | 02 | 03 | 04 | 05 | 06 | 07 | 08 | 09 | 10 | 11 | 12 |
|---:|--:|--:|--:|--:|--:|--:|--:|--:|--:|--:|--:|--:|
| 2024 | 1127 | 1100 | 573 | 476 | 640 | 645 | 744 | 782 | 699 | 620 | 697 | 1097 |
| 2025 | 1460 | 1182 | 616 | 533 | 627 | 519 | 577 | 604 | 459 | 867 | 495 | 785 |

**2026 레거시 대조**

| 월 | 레거시 | DB 기준 | 차이 |
|---|--:|--:|--:|
| 01 | 895 | 901 | +6 |
| 02 | 937 | 920 | -17 |
| 03 | 471 | 473 | +2 |
| 04 | 446 | 446 | 0 |
| **합계** | **2749** | **2740** | **-9** |

> 검토한 후보 중 `EXAM.TODAY_YN`, `OPERATIONDATA.TODAY_YN`, 실제 같은 날 수술(`OPERATION_DATE=EXAM_DATE`), `OP_ESTIMATE`/메모 "원데이" 문구는 레거시 차트와 맞지 않음. `M/5 검사OP + 중단/취소 제외`가 월별/총합 기준 가장 안정적.

**코드 반영 상태**
- [ProcedureExamStatsRepository.java](../../backend/src/main/java/com/bviit/analytics/exam/repository/ProcedureExamStatsRepository.java): `findTotalExamCountMonthly` — EXAM + Cataract_Exam raw `COUNT(*)` UNION ALL 합산 ✅
- [ProcedureExamStatsRepository.java](../../backend/src/main/java/com/bviit/analytics/exam/repository/ProcedureExamStatsRepository.java): `findOneDayExamCountMonthly` — EXAM + 같은 날 `M/5 검사OP` + 중단/취소 제외 ✅
- [ProcedureExamStatsService.java](../../backend/src/main/java/com/bviit/analytics/exam/service/ProcedureExamStatsService.java): 2024/2025 `LEGACY_EXAM_COUNT`, `LEGACY_ONE_DAY_EXAM_COUNT` 고정, 2026~ DB. **캐시 미사용** ✅
- [ProcedureExamStatsController.java](../../backend/src/main/java/com/bviit/analytics/exam/controller/ProcedureExamStatsController.java): `GET /api/stats/procedure-exam/monthly?years=...`, 응답 `ProcedureExamMonthlyItem{year, month, examCount, oneDayExamCount, total}` ✅
- 프론트: [ProcedureExamPage.tsx](../../frontend/src/pages/ProcedureExamPage.tsx) (`검사건수` 메뉴 = `/stats/procedure-exam`), `검사수` / `원데이 검사` 탭 구성.
- **DB 연결**: stats DataSource(`STATS_DB_URL`)를 work → **prod(`220.85.109.247/SOFTCRM`)** 로 교체. 전체 통계가 prod 사용 (`backend/.env`, gitignore).

---

### 1.10 검사자 직업 구성 (직장인 / 학생 / 기타)

**정의**: `검사수`(§1.9.1) 모집단을 검사자 본인의 **직업**으로 3분류한 구성비 지표. 레거시 "총검사자" 표의 `직장인/학생/기타` 칼럼을 재현한다.

| 항목 | 값 |
|------|-----|
| 모집단 | **검사수와 동일** = `EXAM` 행 + `Cataract_Exam` 행 (행 단위, §1.9.1). 3버킷 합 = 검사수 |
| 소스 | **`CUSTOM.JOB`** (검사자 `CUST_NUM`으로 1:1 조인) |
| 분류 단위 | 행(검사 세션). 동일인이 시력교정+백내장 둘 다면 각각 1행으로 직업이 두 번 계수됨(검사수와 동일) |
| 2024~2025 | 레거시 확정값 고정 (DB 재집계 안 함) |
| 2026 이후 | prod DB 계산 |

> **`CUSTOM.JOB`은 코드값이 아니라 자유 입력 텍스트**다(직장인·회사원·사무직·대학생·학생·주부… 수백 종, 공백 다수). 그래서 키워드 매핑 사전으로 3버킷 롤업이 필요하다.

#### 버킷 롤업 규칙

`j = LTRIM(RTRIM(ISNULL(CUSTOM.JOB,'')))` 기준, 위에서부터 우선 적용:

```sql
CASE
  -- ① 직장인 우선 가드: 교사/교수/교직원/강사는 학생 패턴(②)보다 먼저 처리
  --    ("고등학교 교사"가 '%고등%'에 걸려 학생으로 새는 버그 방지)
  WHEN j LIKE N'%교사%' OR j LIKE N'%교수%' OR j LIKE N'%교직원%' OR j LIKE N'%강사%' THEN N'직장인'
  -- ② 학생: 학생/대학원생/수험생/재수생/신입생/초·중·고[숫자]/초·중·고등/대입·대학예정/졸업예정/졸업생
  WHEN j LIKE N'%학생%' OR j LIKE N'%대학원생%' OR j LIKE N'%수험생%'
    OR j LIKE N'%재수생%' OR j LIKE N'%신입생%'
    OR j LIKE N'초[0-9]%' OR j LIKE N'중[0-9]%' OR j LIKE N'고[0-9]%'
    OR j LIKE N'%초등%' OR j LIKE N'%중학%' OR j LIKE N'%고등%' OR j LIKE N'%고3%'
    OR j LIKE N'%대학%예정%' OR j LIKE N'%졸업%예정%' OR j LIKE N'%졸업생%' OR j LIKE N'%대입%'
    OR j = 'university' THEN N'학생'
  -- ③ 군인/대체복무 → 기타 (군인 변형·사회복무요원·공익근무 포함)
  WHEN j LIKE N'%군인%' OR j LIKE N'%사회복무%' OR j LIKE N'%공익근무%' THEN N'기타'
  -- ④ 사무직 → 직장인 (아래 '무직' 부분일치 버그 방지용 선처리)
  WHEN j = N'사무직' THEN N'직장인'
  -- ⑤ 기타: 무직/주부/가사/취업준비/취준/휴식·휴직/퇴직·은퇴/빈값표기(없음·N·None)/공백
  WHEN j IN ('', '.', N'무', N'無', N'무 직', N'무직', N'없음', 'N', 'None', 'none', N'가사', N'주부',
             N'휴식', N'휴식중', N'휴직중', N'취업준비', N'취업준비중', N'취업준비(휴식)', N'취업준비/휴식중',
             N'군입대 직전', N'입대 전', N'기타')
    OR j LIKE N'%주부%' OR j LIKE N'%취업준비%' OR j LIKE N'%취준%'
    OR j LIKE N'%퇴직%' OR j LIKE N'%은퇴%' OR j LIKE N'%휴직%' THEN N'기타'
  -- ⑥ 그 외 전부 → 직장인 (회사원·전문직·자영업/프리랜서 등)
  ELSE N'직장인'
END
```

**핵심 규칙 (레거시 일치의 열쇠)**

| 규칙 | 이유 |
|------|------|
| **군인/대체복무 → 기타** | 레거시가 의무복무·대체복무를 직장인이 아닌 기타로 집계. `%군인%`(군인(진) 등 변형)·사회복무요원·공익근무 포함. (4월 36명이 직장인↔기타 스윙의 주범) |
| **사무직 → 직장인** | `LIKE '%무직%'`이 "사**무직**"을 무직(기타)으로 오분류하던 버그. `=` 선처리로 고정 |
| **교사/교수/교직원/강사 → 직장인 (학생보다 우선)** | "고등학교 교사"가 `%고등%`에 걸려 학생으로 새던 버그. 직장인 가드를 ①에 둬 차단 |
| **재수생·신입생·중[숫자]·고[숫자]·대입·입학/졸업예정·졸업생 → 학생** | 자유입력 경계어. 룰이 직장인으로 보던 학생들 (예: 중2·대입예정자·대입준비). |
| **빈값표기(없음·N·None)·가사 → 기타** | 공백 대신 텍스트로 적힌 "없음/N/None", 가사(주부류)를 기타로. |
| **퇴직·은퇴·휴직·취준 → 기타** | 비근로 상태(퇴직/은퇴/정년퇴직/휴직/취준생)를 직장인에서 분리. |

#### 검증 (2026, prod 실측 — 보정 룰)

| 월 | 직장인 DB/레거시 | 학생 DB/레거시 | 기타 DB/레거시 |
|---|---|---|---|
| 01 | **789 / 789 (0)** | **986 / 986 (0)** | **263 / 263 (0)** |
| 02 | 830 / 834 (−4) | 665 / 671 (−6) | 217 / 216 (+1) |
| 03 | 583 / 582 (+1) | 174 / 177 (−3) | 244 / 248 (−4) |
| 04 | 604 / 602 (+2) | 125 / 127 (−2) | 207 / 203 (+4) |

> **2026-01은 레거시와 완전 일치(789/986/263).** 총 절대오차도 단계적으로 감소(초기 룰 64 → Jan보정 → 퇴직보정 27).
> **핵심: 02~04 잔여 격차는 분류 오류가 아니라 모집단(검사수) 차이다.** 각 달 3버킷 오차합 = 레거시 총검사자(차트값) − 우리 검사수(DB): `02 −9, 03 −6, 04 +4` (§1.9.1 EXAM 스냅샷). 비율로는 전 구간 ±0.7%p 이내라 분류는 이미 정확하며, 모집단이 다르면 ±0은 원천적으로 불가하다. 1월은 모집단이 정확히 일치(2038=2038)해 ±0이 됐다.

#### 함정 (Gotchas)

- **`JOB`는 자유텍스트** — 코드 테이블이 없다. 신규 직업 표현이 계속 유입되므로 `ELSE → 직장인` 기본값이 미분류를 흡수한다. 매핑 사전은 주기적 점검 대상.
- **`'%무직%'` 부분일치 주의** — `사무직`이 걸린다. 반드시 `사무직 = 직장인`을 먼저 처리.
- **`'%고등%'` 부분일치 주의** — `고등학교 교사`가 학생으로 걸린다. 교사/교수/교직원/강사 직장인 가드를 학생 패턴보다 **먼저** 둬야 한다.
- **군인 분류는 정책 결정** — 레거시 일치를 위해 기타로 둔다. 회사 기준 변경 시 이 한 줄만 수정.
- **단위는 행(검사수)** — 사람 수가 아니다. 동일인 복수 검사가 각각 계수된다.

#### 코드 반영 상태

- ⬜ 미반영 — 본 정의는 prod 검증 완료, 화면/Repository 구현 예정. (소개유형 분류는 별도 정의 대기)

---

## 2. 수술건수

수술은 **실제 수술일자** 기준으로 집계한다. 수술예약일/예약등록일이 아니라 실제 수술 실행 테이블을 기준으로 함.

### 2.1 개요

| 카테고리 | 소스 테이블 | 날짜 기준 | 집계 단위 | 비고 |
|---|---|---|---|---|
| **시력교정수술** | `OPERATIONDATA` | `OPERATION_DATE` | **사람** (`DISTINCT CUST_NUM`) | 백내장 수술 고객 제외 |
| **백내장수술** | `Cataract_Operationdata` | `OPERATIONR_DATE` / `OPERATIONL_DATE` | **눈(안)** (`CUST_NUM + 수술일 + R/L`) | 레거시 백내장 수술건수 기준 |

### 2.2 공통 규칙

- 테스트 고객 제외: 고객명 `테스트`, `test` 포함 제외.
- 무효 수술코드 제외: `X`, `OP불가`, `모든수술가능`, `op x`, `Strabismus`, `TEST-TEST`.
- 시력교정은 `Cataract_Operationdata`에 존재하는 고객을 제외하여 백내장 중복 혼입 방지.
- `전체 수술건수`는 세부 수술명 합산이 아니라 `시력교정수술 인원 + 백내장수술 눈 수`.
- 세부 수술명은 양안에 서로 다른 수술명이 들어갈 수 있어 유형별 합계와 전체 인원이 반드시 같지 않음.

### 2.3 시력교정수술

**정의**: 해당 기간 실제 시력교정 수술을 받은 사람 수. 레거시 "시력교정 검사 후 수술" 표와 대조한 결과, 수술예약보다 `OPERATIONDATA.OPERATION_DATE` 실제 수술 기준에 가장 가까움.

```sql
SELECT SUBSTRING(o.OPERATION_DATE,1,7) AS ym,
       COUNT(DISTINCT o.CUST_NUM) AS visionPatients
FROM OPERATIONDATA o WITH(NOLOCK)
JOIN CUSTOM cu WITH(NOLOCK) ON cu.CUST_NUM = o.CUST_NUM
WHERE o.OPERATION_DATE >= :from AND o.OPERATION_DATE <= :to
  AND NOT EXISTS (
      SELECT 1
      FROM Cataract_Operationdata co WITH(NOLOCK)
      WHERE co.CUST_NUM = o.CUST_NUM
  )
  AND (
       (NULLIF(LTRIM(RTRIM(ISNULL(o.OPERATIONR,''))), '') IS NOT NULL
        AND o.OPERATIONR NOT IN ('X','OP불가','모든수술가능','op x','Strabismus','TEST-TEST'))
    OR (NULLIF(LTRIM(RTRIM(ISNULL(o.OPERATIONL,''))), '') IS NOT NULL
        AND o.OPERATIONL NOT IN ('X','OP불가','모든수술가능','op x','Strabismus','TEST-TEST'))
  )
  AND NOT (
      ISNULL(cu.CUST_NAME, '') LIKE N'%테스트%'
      OR LOWER(ISNULL(cu.CUST_NAME, '')) LIKE '%test%'
  )
GROUP BY SUBSTRING(o.OPERATION_DATE,1,7)
ORDER BY ym
```

### 2.4 백내장수술

**정의**: 해당 기간 실제 백내장 수술 눈 수. 우/좌 수술일을 펼친 뒤 `CUST_NUM + OPERATION_DATE + R/L`을 1건으로 집계. 동일인이 같은 날 양안 수술하면 2안으로 집계.

```sql
SELECT SUBSTRING(x.OPERATION_DATE,1,7) AS ym,
       COUNT(DISTINCT x.CUST_NUM + '|' + x.OPERATION_DATE + '|' + x.eye) AS cataractPatients
FROM (
    SELECT CUST_NUM, OPERATIONR_DATE AS OPERATION_DATE, OPERATIONR AS OPERATION, 'R' AS eye
    FROM Cataract_Operationdata WITH(NOLOCK)
    UNION ALL
    SELECT CUST_NUM, OPERATIONL_DATE AS OPERATION_DATE, OPERATIONL AS OPERATION, 'L' AS eye
    FROM Cataract_Operationdata WITH(NOLOCK)
) x
JOIN CUSTOM cu WITH(NOLOCK) ON cu.CUST_NUM = x.CUST_NUM
WHERE x.OPERATION_DATE >= :from AND x.OPERATION_DATE <= :to
  AND NULLIF(LTRIM(RTRIM(ISNULL(x.OPERATION,''))), '') IS NOT NULL
  AND x.OPERATION NOT IN ('X','OP불가','TEST-TEST')
  AND NOT (
      ISNULL(cu.CUST_NAME, '') LIKE N'%테스트%'
      OR LOWER(ISNULL(cu.CUST_NAME, '')) LIKE '%test%'
  )
GROUP BY SUBSTRING(x.OPERATION_DATE,1,7)
ORDER BY ym
```

### 2.5 검증 이력

시력교정수술은 레거시 수기 출력값을 일부 고정값으로 사용. 2024~2025년 1~4월 및 2026년 1~4월은 DB 재집계값 대신 레거시 검수값 사용.

| 연도 | 01 | 02 | 03 | 04 | 비고 |
|---:|---:|---:|---:|---:|---|
| 2024 | 1,617 | 1,578 | 799 | 688 | 레거시 고정 |
| 2025 | 1,878 | 1,533 | 796 | 712 | 레거시 고정 |
| 2026 | 1,361 | 1,388 | 657 | 568 | 레거시 고정 |

**백내장수술 레거시 대조**

| 연도 | 01 | 02 | 03 | 04 | 05 | 비고 |
|---:|---:|---:|---:|---:|---:|---|
| 2024 | 76 | 64 | 53 | 50 | 65 | 레거시 백내장 수술건수 |
| 2025 | 74 | 68 | 76 | 59 | 55 | 레거시 백내장 수술건수 |
| 2026 | 96 | 68 | 76 | 58 | - | 레거시 백내장 수술건수 |

> 수술자 리스트의 백내장은 `CUST_NUM + OPERATION_DATE` 행 단위라 같은 날 양안 수술을 1행으로 보여줄 수 있음. 반면 백내장 수술건수는 회의 기준상 **눈(안) 단위**라 우/좌를 각각 1건으로 세어 수술자 리스트 행수보다 커질 수 있음.

### 2.6 코드 반영 상태

- [SurgeryStatsRepository.java](../../backend/src/main/java/com/bviit/analytics/surgery/repository/SurgeryStatsRepository.java):
  - 시력교정: `OPERATIONDATA.OPERATION_DATE`, 백내장 수술 고객 제외, 테스트 고객 제외 ✅
  - 백내장: `Cataract_Operationdata` 우/좌 수술일 펼침, `CUST_NUM + OPERATION_DATE + R/L` 눈 단위 ✅
- [SurgeryStatsService.java](../../backend/src/main/java/com/bviit/analytics/surgery/service/SurgeryStatsService.java):
  - 2024~2025년 1~4월, 2026년 1~4월 시력교정수술 레거시 검수값 고정 ✅
  - 기본 JSON 캐시 사용. 캐시 파일이 없으면 DB 쿼리 후 `.cache/stats/surgery-monthly-v3-*.json` 저장.
- `/stats/surgery` 화면의 `전체 수술건수`는 세부 유형 합산이 아니라 `visionPatients + cataractPatients`.

---

## 3. 산출물 저장 전략

대시보드 지표는 **2026년 이후 데이터부터 일별 산출물 저장 → 월/주/기간 화면은 일별 합산**을 원칙으로 한다.

### 3.1 기준

| 기간 | 저장/집계 방식 | 이유 |
|---|---|---|
| **2024~2025년** | 레거시 월별 확정값 사용 | 일별 원본 검수값이 없고, 병원 공식 월별 값이 기준 |
| **2026년 이후** | 일별 산출물 저장 후 합산 | 주차/월/사용자 기간 검색을 같은 기준으로 처리하고 차이 원인 추적 가능 |

### 3.2 대상 지표

Airflow 계열 함수명 기준으로 아래 6개 일별 산출물을 우선 대상으로 둔다.

| 센터 | 검사 | 수술 | 매출 |
|---|---|---|---|
| **시력교정센터** | `get_daily_exam_count_result` | `get_daily_operation_count_result` | `get_daily_revenue_result` |
| **백내장** | `get_daily_cataract_exam_count_result` | `get_daily_cataract_operation_count_result` | `get_daily_cataract_revenue_result` |

### 3.3 저장 단위

권장 저장 형태:

```text
daily_metric_result
- metric_date       -- YYYY-MM-DD
- center_type       -- vision | cataract
- metric_type       -- exam_count | operation_count | revenue
- value             -- 산출값
- unit              -- person | eye | won
- rule_version      -- 산출 기준 버전
- generated_at      -- 산출 시각
```

운영 원칙:

- 화면의 월별 값은 `metric_date` 월 범위 합산으로 만든다.
- 화면의 주차/퀵버튼/사용자 기간 값도 동일한 일별 산출물을 합산한다.
- 2024~2025년 레거시 고정값은 월별 기준값으로만 보관한다. 임의 일별 배분 금지.
- 2026년 이후 일별 산출물은 산출 기준 변경 시 `rule_version`을 올려 재생성한다.
- `EXAM`처럼 사후 변경되는 스냅샷 테이블은 일별 산출물을 저장해 월별 숫자 흔들림을 추적한다.

### 3.4 현재 코드와의 관계

현재 Java Dashboard는 검사/수술을 **월별 직접 집계 + JSON 캐시**로 처리한다.

| 지표 | 현재 구현 | 향후 방향 |
|---|---|---|
| 검사건수 | 월별 쿼리 + `.cache/stats/examination-monthly-v7-*` | 2026년 이후 일별 검사 산출물 저장 후 합산 |
| 수술건수 | 월별 쿼리 + `.cache/stats/surgery-monthly-v3-*` | 2026년 이후 일별 수술 산출물 저장 후 합산 |
| 매출 | B2B 매출 일부 구현 | 일반 시력교정/백내장 일별 매출 기준 별도 정의 필요 |

---

## 4. 외래수

- **2024~2025**: 레거시 차트 확정값 고정.
- **2026년 이후**: 운영 DB 산출값. 외래 예약(`RESERVE_FLAG='F'`) 중 내원/퇴원 처리된(`RESERVE_STATE IN ('I','H')`) **행 수**(`COUNT(*)`, 사람 단위 아님).

### 4.1 정의

| 항목 | 값 |
|---|---|
| 화면 | `/stats/outpatient-count` |
| 메뉴 | `외래 > 외래수` |
| 소스 | `RESERVATION` (`RESERVE_FLAG='F'`, `RESERVE_STATE IN ('I','H')`) |
| 날짜 기준 | `RESERVE_DATE` (char(10), `YYYY-MM-DD`) |
| 집계 단위 | **행 수** (`COUNT(*)`) |
| 2024~2025 | 레거시 확정값 고정 (DB 재집계 안 함) |
| 2026 이후 | DB 라이브 계산 (mssql 프로파일) |

```sql
SELECT SUBSTRING(r.RESERVE_DATE,1,7) AS ym, COUNT(*) AS cnt
FROM RESERVATION r WITH(NOLOCK)
WHERE r.RESERVE_DATE >= :from AND r.RESERVE_DATE <= :to
  AND r.RESERVE_FLAG = 'F'
  AND r.RESERVE_STATE IN ('I','H')
GROUP BY SUBSTRING(r.RESERVE_DATE,1,7)
ORDER BY ym
```

### 4.2 값

**2024~2025 레거시 확정값**

| 연도 | 01 | 02 | 03 | 04 | 05 | 06 | 07 | 08 | 09 | 10 | 11 | 12 |
|---:|--:|--:|--:|--:|--:|--:|--:|--:|--:|--:|--:|--:|
| 2024 | 5257 | 5080 | 4721 | 4133 | 4159 | 4092 | 4760 | 4720 | 3696 | 4201 | 3948 | 4711 |
| 2025 | 5389 | 5389 | 4933 | 4349 | 4451 | 4000 | 4275 | 4354 | 3332 | 3724 | 3639 | 4086 |

**2026 DB 검증 (운영 DB 실계산, 5월 이후는 라이브)**

| 월 | 레거시(참고) | DB 계산 | 차이 |
|---|--:|--:|--:|
| 01 | 4822 | 4831 | +9 |
| 02 | 4339 | 4344 | +5 |
| 03 | 4247 | 4251 | +4 |
| 04 | 3653 | 3658 | +5 |

> DB 계산이 레거시보다 월 4~9건 높다. 테스트 고객/예약번호/메모를 제외해도 거의 동일 → 레거시 출력 시점 이후 상태값 변경 또는 일부 외래 세부구분 차이로 추정. **레거시가 아니라 DB 라이브 값을 공식 표시값으로 사용**한다.

### 4.3 함정 (Gotchas)

- **2025-02 세부구분 이슈**: 레거시 5,389 vs DB 5,673 (+284). `RESERVE_JINRYO='3'`이 294건이라 거의 원인처럼 보이나, 이 코드를 전역에서 제외하면 다른 월이 크게 틀어진다 → **전역 규칙으로 쓰지 말 것**. 2024~2025는 레거시 고정이므로 이 차이는 표시에 영향 없음.
- **사람 수 아님**: 행 수 기준이라 동일인 복수 외래 예약이 각각 1건으로 잡힌다.

### 4.4 코드 반영 상태

- [OutpatientCountStatsRepository.java](../../backend/src/main/java/com/bviit/analytics/outpatient/repository/OutpatientCountStatsRepository.java): `findOutpatientCountMonthly` — `F` + `I/H` 행 수 ✅
- [OutpatientCountStatsController.java](../../backend/src/main/java/com/bviit/analytics/outpatient/controller/OutpatientCountStatsController.java): `GET /api/stats/outpatient-count/monthly?years=...` ✅
- [OutpatientCountStatsService.java](../../backend/src/main/java/com/bviit/analytics/outpatient/service/OutpatientCountStatsService.java): 2024~2025 레거시 고정 + 2026~ DB(`Optional<Repository>`, mssql만 계산) ✅
- [OutpatientCountPage.tsx](../../frontend/src/pages/OutpatientCountPage.tsx): `외래수` 월별 추이 화면 ✅
- 메뉴 위치: `수술`과 `마케팅` 사이에 `외래` 그룹 추가 ✅

---

## 5. 예약 (종합 · 온라인 · 콜)

### 5.1 예약 종합 (콜 · 온라인)

레거시 BCRM "문의통계(RSS)" 화면(`uSTATISTICSD1`)의 검사예약 채널 집계를 재현. **한 화면 3탭**: 예약 종합(콜+온라인) · 온라인 예약(네이버·카카오·홈페이지) · 콜 예약(인콜·아웃콜).

- **2024~2025**: 레거시 차트 확정값 고정.
- **2026년 이후**: 운영 DB 추정치. **등록일(`InsertedDateTime`) 기준** — RSS 화면이 등록일로 집계하기 때문. 예약일(`RESERVE_DATE`)로 세면 타월 예약 혼입으로 차트와 어긋남(특히 2월 +20%).

#### 5.1.1 정의

| 항목 | 값 |
|---|---|
| 화면 | `/stats/reservation-overall` (메뉴: `예약 > 예약 종합`, 3탭) |
| 소스 | `RESERVATION` (`RESERVE_FLAG='M'`, `RESERVE_JINRYO IN ('','5','6','7','24','25')`) |
| 날짜 기준 | **`InsertedDateTime`(등록일)**. 미래 불가 → 당월까지 자연 완결, 이후 월 null |
| **콜** | `RESERVE_PATH IN ('CTI','CRM')` (인콜=CTI·아웃콜=CRM). **B2B(군인)·재검·중복 제외**: `RESERVE_SEQ NOT IN ('8','5')` + `COMMENT NOT LIKE` `%B2B(군인)%`/`%재검%`/`%중복%`/`%시뮬%`/`%차트있음%` |
| **온라인** | `RESERVE_PATH IN ('ONLINE','APP','NAVER')` (홈페이지=ONLINE·APP, 네이버=NAVER). **제외 미적용**(B2B군인이 온라인엔 거의 없어 적용 시 과소) |
| **종합** | 콜 + 온라인 (경로 상호배타 → `union DISTINCT`) |
| 집계 단위 | `COUNT(DISTINCT RESERVE_NUM)` |
| 2024~2025 | 레거시 확정값 고정 (DB 재집계 안 함) |
| 2026 이후 | DB 라이브 (mssql 프로파일) |

```sql
-- callFilter (콜 채널, B2B군인·재검·중복 제외):
--   r.RESERVE_PATH IN ('CTI','CRM')
--   AND ISNULL(r.RESERVE_SEQ,'') NOT IN ('8','5')
--   AND ISNULL(r.COMMENT,'') NOT LIKE '%B2B(군인)%' AND ... NOT LIKE '%재검%'/'%중복%'/'%시뮬%'/'%차트있음%'
SELECT YEAR(r.InsertedDateTime) AS yr, MONTH(r.InsertedDateTime) AS mo,
  COUNT(DISTINCT CASE WHEN (callFilter) OR r.RESERVE_PATH IN ('ONLINE','APP','NAVER')
                      THEN r.RESERVE_NUM END) AS 종합,
  COUNT(DISTINCT CASE WHEN r.RESERVE_PATH IN ('ONLINE','APP','NAVER') THEN r.RESERVE_NUM END) AS 온라인,
  COUNT(DISTINCT CASE WHEN (callFilter) THEN r.RESERVE_NUM END) AS 콜
FROM RESERVATION r WITH(NOLOCK)
WHERE r.InsertedDateTime >= :from AND r.InsertedDateTime <= :to
  AND r.RESERVE_FLAG='M' AND r.RESERVE_JINRYO IN ('','5','6','7','24','25')
  AND NOT (r.CUST_NAME LIKE '%테스트%' OR r.CUST_NAME LIKE '%TEST%' OR r.CUST_NUM='8888888888888')
GROUP BY YEAR(r.InsertedDateTime), MONTH(r.InsertedDateTime)
```

#### 5.1.2 값 (2024~2025 레거시 확정값)

**예약 종합 (콜+온라인)**

| 연도 | 01 | 02 | 03 | 04 | 05 | 06 | 07 | 08 | 09 | 10 | 11 | 12 |
|---:|--:|--:|--:|--:|--:|--:|--:|--:|--:|--:|--:|--:|
| 2024 | 2799 | 1651 | 1174 | 1092 | 1264 | 1317 | 1569 | 1641 | 1206 | 1231 | 1967 | 2589 |
| 2025 | 2759 | 1945 | 1204 | 1357 | 1219 | 1318 | 1581 | 1315 | 1645 | 1031 | 1473 | 2209 |

**온라인 예약 (홈페이지·네이버·카카오)**

| 연도 | 01 | 02 | 03 | 04 | 05 | 06 | 07 | 08 | 09 | 10 | 11 | 12 |
|---:|--:|--:|--:|--:|--:|--:|--:|--:|--:|--:|--:|--:|
| 2024 | 1466 | 943 | 707 | 599 | 575 | 578 | 716 | 879 | 672 | 662 | 1001 | 1292 |
| 2025 | 1334 | 878 | 548 | 583 | 501 | 529 | 608 | 551 | 791 | 477 | 573 | 975 |

**콜 예약 (인콜·아웃콜)**

| 연도 | 01 | 02 | 03 | 04 | 05 | 06 | 07 | 08 | 09 | 10 | 11 | 12 |
|---:|--:|--:|--:|--:|--:|--:|--:|--:|--:|--:|--:|--:|
| 2024 | 1333 | 708 | 467 | 493 | 689 | 739 | 853 | 762 | 534 | 569 | 966 | 1297 |
| 2025 | 1425 | 1067 | 656 | 774 | 718 | 789 | 973 | 764 | 854 | 554 | 900 | 1234 |

> 검산: 콜 + 온라인 = 종합 (예: 2024-01 1333+1466=2799 ✓).

#### 5.1.3 2026 DB 검증 (등록일 기준 · 차트=레거시 스냅샷)

| 월 | 종합 DB / 차트 | 콜 DB / 차트 | 온라인 DB / 차트 |
|---|---|---|---|
| 01 | 2410 / 2425 | 1434 / 1432 | 976 / 993 |
| 02 | 1902 / 1892 | 1138 / 1132 | 764 / 760 |
| 03 | 1301 / 1318 | 790 / 836 | 511 / 482 |
| 04 | 1256 / 1243 | 768 / 750 | 488 / 493 |

> 위 보정(등록일 + 콜 B2B제외 + 종합=콜+온라인)으로 **월 ±1~3% 일치**. 잔차는 차트가 특정 시점 수기 export라 생기는 스냅샷 드리프트로, 필터로 더 줄지 않음(온라인 오차는 평균 +3·부호 양쪽 = 노이즈). **레거시가 아니라 DB 라이브 값을 표시값으로 사용**.

#### 5.1.4 함정 (Gotchas)

- **레거시 공식값 완전 재현 불가**: 원본 `LoadData1`은 콜센터 MySQL(`OPENQUERY(EICN_MySQL,...)` 인입콜)·`CtiRptLst`·`DB_CUSTOM`(TM)·해피톡 등 다중 소스 + 등록일 기준. 위 정의는 RESERVATION 경로 기반 **근접 추정치**.
- **RSS 수기 테이블은 폐기됨**: 네이버/카카오 수기 입력 테이블 `[220.85.109.240].MCRM.dbo.OnLineReservation`은 1행(2024-07, 삭제)뿐, 인입콜 캐시 `RSS`도 31행(2025-08~11)뿐. 현재 네이버는 `RESERVE_PATH='NAVER'`로 통합됨.
- **카카오는 `RESERVE_PATH` 없음**: 해피톡 채널이라 RESERVATION에 거의 안 잡힘(`HappyTalk_Counsel_List` ★신환 월 9~16건). 온라인 합산엔 사실상 미포함이나 차트도 비슷.
- **렌삽(JINRYO 24/25)·취소(STATE='C')는 손대지 말 것**: 렌삽은 `FLAG='M'`엔 0건이라 무관, 취소 제외는 과도(−376)라 차트와 멀어짐.
- **B2B(군인) 제외는 콜에만**: `RESERVE_SEQ='8'` 군인 단체예약 월 ~80건이 콜로 유입. 온라인에 같은 제외 적용 시 과소.
- **EICN_MySQL 링크드서버는 우리 연결에서 작동**: 진짜 공식값이 필요하면 `OPENQUERY(EICN_MySQL,...)`로 인입콜까지 재현 가능(별도 계정 불필요).

#### 5.1.5 코드 반영 상태

- [ReservationOverallStatsRepository.java](../../backend/src/main/java/com/bviit/analytics/reservation/repository/ReservationOverallStatsRepository.java): 등록일 기준 종합/온라인/콜 한 쿼리 ✅
- [ReservationOverallStatsService.java](../../backend/src/main/java/com/bviit/analytics/reservation/service/ReservationOverallStatsService.java): 2024~25 확정값(`LEGACY_*` 3맵) + 2026~ DB, 미래월 null ✅
- [ReservationOverallStatsController.java](../../backend/src/main/java/com/bviit/analytics/reservation/controller/ReservationOverallStatsController.java): `GET /api/stats/reservation-overall/monthly?years=...` ✅
- [ReservationOverallPage.tsx](../../frontend/src/pages/ReservationOverallPage.tsx): 3탭(종합/온라인/콜) 월별 추이 ✅ · 메뉴: `예약 > 예약 종합`

### 5.2 예약건수

> ⬜ TODO — `RESERVATION` `RESERVE_STATE` 기반 정의 예정(`/stats/reservation`).

---

## 6. 검사자 종합표 (35칼럼)

레거시 "총검사자" 종합표를 그대로 재현하는 월별 표. **9개 블록 / 35칼럼** 전체를 다룬다(연·월 제외).

### 6.1 데이터 공급 전략

| 기간 | 공급 방식 |
|------|-----------|
| **2024~2025** | 레거시 표 값 **그대로 고정** (원본 엑셀 TSV 기준, DB 재집계 안 함) |
| **2026~** | 운영 DB 조사값과 파생 산식으로 산출 가능한 칼럼 반영. 미정 칼럼은 공란 유지(칼럼별 소스는 §6.3) |

> **값 보관 위치**: 2024~2025 확정값과 2026 정적 조사값은 [OverallExamPage.tsx](../../frontend/src/pages/OverallExamPage.tsx)의 `ROWS` 상수에 보존한다. 2026-06 행은 2026-06-15 기준 월중 부분 집계다.

### 6.2 칼럼 사전 (9블록 / 35칼럼)

| # | 블록 | 칼럼 | 단위 | 2026 소스 / 비고 |
|--:|------|------|------|------|
| 1 | (총검사자) | 총검사자 | 건 | **검사수** §1.9.1 |
| 2 | 소개유형 | 일반고객 · 고객소개 · 직원소개 | 명 | `MOTIVE_NEW02` (§6.3). 합 = 총검사자 |
| 3 | 직업 | 직장인 · 학생 · 기타 | 명 | `CUSTOM.JOB` 롤업 §1.10. 합 = 총검사자 |
| 4 | 시력교정 수술예약 | 예약 · 예약률(중단포함) | 건 / % | 시력교정 수술예약·예약률 (정의 ⬜TODO) |
| 5 | 백내장 | 전체(노안포함) · 만 · 중단제외 · 예약 · 예약률 · 전환률 | 건 / % | §1.4·1.4.1 연관. **중단제외는 정의 미확정으로 2026 공란 유지** |
| 6 | 중단 | 중단수 · 중단율 | 건 / % | **중단수**=`EXAM.STOP_YN='Y'` 월별. **중단율**=중단수÷검사건수 시력교정(레거시 검산 일치). ⚠️ EXAM 스냅샷이라 과거월 사후 변동 |
| 7 | 검사 건수 | 시력교정 · 드림렌즈 · 백내장 · 노안2 · 합계 | 건 | §1.3/§1.5/§1.4. **노안2 = 백내장 `전체(노안포함) − 만`**(블록5 idx9−idx10). **합계 = 시력교정+드림렌즈+백내장+노안2** |
| 8 | 검사 비율 | 시력교정 · 드림렌즈 · 노안 · 백내장 · 노안2+5대질환 | % | 블록7의 비율. **분모 = `합계`** |
| 9 | 원데이/일반검사 | 시력교정 · 원데이 · 원데이예약 · 일반검사 · 일반검사예약 · 일반검사비율 · 원데이성공률 · 일반성공률 | 건 / % | 원데이 §1.9.2. 원데이예약=원데이 후 7일 이내 유효 시력교정 수술 존재. 일반검사=시력교정−원데이, 일반검사예약=시력교정 수술예약−원데이예약 |

> **검산(2024~2025 전월 통과)**: `일반+고객소개+직원소개 = 총검사자`, `직장인+학생+기타 = 총검사자`.

### 6.3 2026 산출 시 주의 (정합성)

- **총검사자**: §1.9.1 검사수 그대로. ✅ 산출 가능
- **직업(직장인/학생/기타)**: §1.10 룰. prod 4개월 검증 레거시 ±1.5%. ✅ 산출 가능
- **검사 건수 시력교정·드림렌즈**: §1.3/1.5. ✅ 운영 DB 라이브 산출. **합계 = 시력교정+드림렌즈+백내장+노안2**.
- **백내장 funnel(전체(노안포함)·만·노안2) + 검사건수 '백내장'**: 정의 확정 — **전체** = `Cataract_Exam` 전체 검사(≈백내장 리스트 조회건수), **만** = 백내장 검사건수(§1.4, 검사건수 블록 '백내장'과 **동일값으로 통일**), **노안2** = 전체−만. **현재 OverallExamPage 2026은 우리 DB 기준 조회값**(전체 라이브 145/100/128/110/132/64, 만 §1.4)을 정적 입력해 표시한다. ⚠️ `Cataract_Exam`도 스냅샷이라 **레거시 엑셀 마감값과 과거월이 다르다**(엑셀 전체 153/106/135/110 — 04월만 일치, 01~03 −6~8). 엑셀 확정값 고정은 **추후 월 마감 freeze**로(§1.9.1.1·§3). 예약률=예약÷만.
- **중단수·중단율**: 중단수=`EXAM.STOP_YN='Y'`, 중단율=중단수÷시력교정. ✅ 산출 가능
- **검사 비율**: 분모는 검사 건수 `합계`. 2026은 시력교정·드림렌즈·백내장·노안2+5대질환 비율을 반영하고, 별도 `노안` 분류값은 현재 0.0%로 둔다.
- **원데이/일반검사**: ✅ 산출 가능. `원데이`는 §1.9.2. `원데이예약`은 이름과 달리 단순 수술예약(`O`) 존재가 아니라, 레거시 표와 대조 시 **원데이 검사 후 7일 이내 유효 시력교정 수술 존재**가 가장 가깝다. 2026-01~04는 레거시 스냅샷값(804/839/422/381)을 우선 반영했고, 05~06은 같은 기준으로 운영 DB에서 산출했다. `일반검사=시력교정−원데이`, `일반검사예약=시력교정 수술예약−원데이예약`, `일반검사비율=일반검사÷시력교정`, `원데이성공률=원데이예약÷원데이`, `일반성공률=일반검사예약÷일반검사`.

**원데이예약 원인 조사 (2026-01~04)**:

| 기준 | 01 | 02 | 03 | 04 | 판정 |
|---|--:|--:|--:|--:|---|
| 화면/레거시 스냅샷 | 804 | 839 | 422 | 381 | 적용값 |
| 단순 수술예약 O 존재 | 842 | 866 | 440 | 411 | 과대(+18~38) |
| 당일 수술예약/당일 실제수술 | 778 | 808 | 409 | 371 | 과소 |
| 유효 시력교정 수술 7일 이내(현재 DB) | 810 | 839 | 428 | 388 | 최접근. 잔차는 EXAM/OPERATIONDATA 스냅샷·사후 입력 차이로 판단 |
- **소개유형(일반/고객소개/직원소개)**: `MOTIVE_NEW02.category01_name` — **고객소개=`소개고객`+`소개미확인`**, 직원소개=`소개직원`, 일반=잔차. 레거시가 `소개미확인`(소개자정보 0%)을 고객소개로 편입한 것을 2026-06-22 prod 검증으로 확인해 동일 적용 → 레거시 월간보고와 정합(2026 1~4월 평균차 고객소개 ~13건·일반 ~10건, 기존 ~110/~106건에서 수렴). EXAM 덮어쓰기 스냅샷 특성상 과거월은 소폭 소급변동 가능.
- **남은 공란**: 백내장 `중단제외`는 2026 산출 기준 미확정으로 공란 유지.

**함정**:
- **`합계` ≠ 총검사자** — 검사 건수 블록은 별도 집계라 다를 수 있다(예: 2024-04 합계 1,167 vs 총검사자 1,140).
- **검사 비율 5칼럼 ≠ 검사 건수 4칼럼** — 비율 블록은 `노안`을 별도로 두어 분류가 다르다. 분모는 `합계`.

### 6.4 코드 반영 상태

- 🟡 화면 반영 — [OverallExamPage.tsx](../../frontend/src/pages/OverallExamPage.tsx): 35칼럼 정적 표(2024~25 레거시 값), 가로 스크롤·2단 헤더·연/월 sticky. `전체지표 > 검사자` 메뉴(`/stats/overall-exam`).
- 🟡 2026 부분 반영(정적 값) — 총검사자·소개유형·직업·시력교정 수술예약·백내장 funnel(전체/만/예약/예약률/전환률)·중단수·중단율·검사건수(시력교정/드림렌즈/백내장/노안2/합계)·검사비율·원데이/일반검사 블록 채움. 백내장 `중단제외`만 공란 유지.
- ⬜ 월별 표 백엔드 API·2026 DB 라이브 산출 미착수(현재 prod 조사값을 정적 입력). 백내장 `중단제외` 정의 미확정(§6.3).
- ✅ **주간 표는 운영 DB 라이브** — [OverallExamWeeklyPage.tsx](../../frontend/src/pages/OverallExamWeeklyPage.tsx) (`전체지표 > 주간 검사자 종합지표` 메뉴, `/stats/overall-exam-weekly`). 백엔드 `GET /api/stats/overall-exam/weekly?years=` ([OverallExamWeeklyController](../../backend/src/main/java/com/bviit/analytics/overall/controller/OverallExamWeeklyController.java)·Service·Repository). 일자별 집계를 **주(월~일)로 버킷팅**한다.
  - **주 정의**: 월요일~일요일, 월 경계를 걸친 주는 **각 달로 잘라 귀속**. **1일~첫 월요일 직전의 선행 부분 구간은 첫 정규 주에 합쳐 1주로 만든다**(1주 = 1일~첫 일요일, 마지막 주만 부분 주). 검증: 2026-02(1일=일) → 1주 02/01~08·2주 09~15·…·4주 23~28(부분), 2026-05(1일=금) → 1주 05/01~10·…, 모두 주 합계 = 월 검사수 일치(05=1054).
  - **전 구간 DB 라이브**(2024~25 레거시 고정값은 월 단위라 주로 분해 불가). SQL 본문은 §1.3~1.10 월별 쿼리와 동일하고 집계 단위만 일자로 바꿈. `중단제외`·백내장 `전환률`은 월별과 동일하게 공란. **소개유형**은 우리 DB 기준(MOTIVE_NEW02 `소개고객`)이라 레거시와 차이 있을 수 있음(§6.3).

---

## 관련 문서

- [검사자리스트-컬럼정의.md](검사자리스트-컬럼정의.md) — "검사자 리스트(상담사별)" 화면 78컬럼 ↔ DB 매핑
- [tables/RESERVATION.md](tables/RESERVATION.md) — 예약 테이블 상세
- [tables/Cataract_Exam.md](tables/Cataract_Exam.md) — 백내장 검사 상세 (좌/우안 컬럼)
- [tables/EXAM.md](tables/EXAM.md) — 검사 결과 스냅샷(고객당 1행)
- [../../CLAUDE.md](../../CLAUDE.md) — 통계 페이지 목록 및 컨벤션
